# AI Interaction Protocol
You are analyzing a stripped-down codebase skeleton. 
- Do not write implementation code yet.
- Analyze the user's issue against the File Tree and Skeletons below.
- Identify the exact files containing the logic you need to inspect.
- Ask the user to provide the full contents of those specific files (limit your request to 1–3 files at a time).
- Once the user provides the requested files, proceed to solve the problem.

# Codebase Workspace Structure

## File Tree
```text
.
└── README.md
├── agent-backend/
│   └── bun.lock
│   ├── llm/
│   │   └── orchestrator.ts
│   │   └── prompts.ts
│   │   └── tools.ts
│   └── package-lock.json
│   └── package.json
│   └── server.ts
│   ├── src/
│   │   └── connectionManager.ts
│   │   ├── handlers/
│   │   │   └── statusHandler.ts
│   │   │   └── voiceHandler.ts
│   │   └── messageRouter.ts
│   │   ├── schema/
│   │   │   ├── forms/
│   │   │   │   └── CreateAggrement.json
│   │   │   │   └── createHotel.json
│   │   │   │   └── createIssue.json
│   │   │   │   └── createOrder.json
│   │   │   └── loader.ts
│   │   │   └── routeRegistry.json
│   │   ├── services/
│   │   │   └── elevenLabsSTT.ts
│   │   │   └── elevenLabsTTS.ts
│   │   │   └── responseSender.ts
│   │   │   └── sttService.ts
│   │   │   └── ttsService.ts
│   │   │   └── voicePipeline.ts
│   │   ├── state/
│   │   │   └── stateMachine.ts
│   │   └── types.ts
│   │   ├── walkthrough/
│   │   │   └── driver.ts
│   │   │   └── narrationService.ts
│   │   │   └── narrationTemplates.ts
│   │   │   └── planner.ts
│   │   │   └── sessionManager.ts
│   │   │   └── statusAwaiter.ts
│   │   │   └── toolMessenger.ts
│   └── test-client.ts
│   └── test-state-machine.ts
│   ├── tests/
│   │   └── voiceHandler.acceptance.test.ts
│   │   └── walkthroughDriver.acceptance.test.ts
│   └── tsconfig.json
│   ├── utils/
│   │   └── keywordMatcher.ts
│   ├── voice/
│   │   └── stt.ts
│   │   └── tts.ts
└── eslint.config.js
└── index.html
└── package-lock.json
└── package.json
├── public/
│   └── flash.png
│   └── linen.png
├── src/
│   └── App.css
│   └── App.jsx
│   ├── agent/
│   │   └── AgentBridge.jsx
│   │   └── AgentErrorBoundary.jsx
│   │   └── AgentOverlay.jsx
│   │   └── AudioQueue.js
│   │   └── NavigationHandler.jsx
│   │   └── SpotlightManager.js
│   │   └── WalkthroughHandler.jsx
│   │   └── agentFormRegistry.js
│   │   ├── fieldFiller/
│   │   │   └── buttonHelper.js
│   │   │   └── dialogHelper.js
│   │   │   └── fillAutocomplete.js
│   │   │   └── fillSelect.js
│   │   │   └── fillText.js
│   │   │   └── fillToggle.js
│   │   │   └── finder.js
│   │   │   └── index.js
│   │   │   └── nativeSetValue.js
│   │   └── formExecutor.js
│   │   └── messageRouter.js
│   │   └── protocol.js
│   │   └── spotlight.css
│   │   ├── toolHandlers/
│   │   │   └── actionTools.js
│   │   │   └── begin_walkthrough.js
│   │   │   └── fieldTools.js
│   │   │   └── index.js
│   │   └── toolRegistry.js
│   │   └── useAgentForm.js
│   │   └── utils.js
│   │   └── wsConnection.js
│   ├── assets/
│   │   └── react.svg
│   ├── components/
│   │   ├── InventoryReservation/
│   │   │   └── CreateReservationDialog.jsx
│   │   │   └── CreateTransactionDialog.jsx
│   │   │   └── CustomerTransactionDrawer.jsx
│   │   │   └── InventoryReservation.jsx
│   │   ├── RouteManagement/
│   │   │   └── AssignCustomersDialog.jsx
│   │   │   └── CreateRouteDialog.jsx
│   │   │   └── RouteDrawer.jsx
│   │   │   └── RouteList.jsx
│   │   │   └── RouteManagementPage.jsx
│   │   ├── Scanner/
│   │   │   └── ColumnScanHeaderAction.jsx
│   │   │   └── QuantityScanInput.jsx
│   │   │   └── ScannedIdsIndicator.jsx
│   │   │   └── ScannerController.jsx
│   │   │   └── ScannerHeader.jsx
│   │   │   └── ScannerPicker.jsx
│   │   ├── auth/
│   │   │   └── Login.jsx
│   │   │   └── login.css
│   │   ├── billing/
│   │   │   └── BillManagementPage.jsx
│   │   │   └── BillingCycle.jsx
│   │   │   └── BillingPreference.jsx
│   │   │   ├── dialogs/
│   │   │   │   └── AddPreferenceDialog.jsx
│   │   │   │   └── ProcessOrdersDialog.jsx
│   │   │   ├── drawers/
│   │   │   │   └── BillingCycleSidebar.jsx
│   │   │   │   └── BillingPreferenceSidebar.jsx
│   │   ├── common/
│   │   │   └── ConditionChip.jsx
│   │   │   └── ConfirmDialog.jsx
│   │   │   └── CustomDrawer.jsx
│   │   │   └── GreenButton.jsx
│   │   │   └── ImageUploadPanel.jsx
│   │   │   └── StatusChip.jsx
│   │   │   └── TableCell.jsx
│   │   │   └── TabsHeader.jsx
│   │   │   ├── tables/
│   │   │   │   └── DataRow.jsx
│   │   │   │   └── DataTable.jsx
│   │   ├── customers/
│   │   │   └── CustomerDetails.jsx
│   │   │   └── CustomerFormDialog.jsx
│   │   │   └── CustomerList.jsx
│   │   │   └── CustomerTable.jsx
│   │   │   └── CustomerUserDialog.jsx
│   │   │   └── CustomerUsersTable.jsx
│   │   │   └── CustomersPage.jsx
│   │   ├── damageAssessment/
│   │   │   └── DamageAssessmentDashboard.jsx
│   │   │   └── DamageAssessmentSidebar.jsx
│   │   │   └── ItemDamageRequestDialog.jsx
│   │   │   ├── create/
│   │   │   │   └── DamageItemPanel.jsx
│   │   │   │   └── DamageSourcePanel.jsx
│   │   ├── dashboard/
│   │   │   └── ChartCard.jsx
│   │   │   └── ChartSkeleton.jsx
│   │   │   └── Dashboard.jsx
│   │   │   └── EmptyState.jsx
│   │   │   └── ErrorState.jsx
│   │   │   └── LoaderScreen.jsx
│   │   │   └── MetricCard.jsx
│   │   │   └── Section.jsx
│   │   ├── hotels/
│   │   │   └── HotelDialog.jsx
│   │   │   └── HotelList.jsx
│   │   │   └── HotelsPage.jsx
│   │   ├── inventory/
│   │   │   └── BulkSearchDialog.jsx
│   │   │   └── InventoryDetails.jsx
│   │   │   └── InventoryList.jsx
│   │   │   └── InventoryPage.jsx
│   │   │   └── InventoryPoolItemDetails.jsx
│   │   ├── inventory-pool/
│   │   │   └── AddTransactionDialog.jsx
│   │   │   └── CreatePoolDialog.jsx
│   │   │   └── CustomerInventoryDrawer.jsx
│   │   │   └── InventoryPoolPage.jsx
│   │   │   └── InventoryPoolTable.jsx
│   │   │   └── InventoryPoolTransactionDrawer.jsx
│   │   │   └── InwardInventoryItemsToPoolDialog.jsx
│   │   ├── inventoryItemReservation/
│   │   │   └── InventoryItemReservtionPage.jsx
│   │   ├── inventorySummary/
│   │   │   └── CreateInwardRequestInventoryDialog.jsx
│   │   │   └── InventorySummaryPage.jsx
│   │   │   └── InventorySummaryTable.jsx
│   │   │   └── InventoryTabs.jsx
│   │   │   └── InwardRequestDrawer.jsx
│   │   │   └── InwardRequestsTable.jsx
│   │   ├── invoices/
│   │   │   └── AddPaymentDialog.jsx
│   │   │   └── CreateInvoiceDialog.jsx
│   │   │   └── InvoiceDetails.jsx
│   │   │   └── InvoiceFormDialog.jsx
│   │   │   └── InvoiceGenerator.jsx
│   │   │   └── InvoicesPage.jsx
│   │   ├── issueTracker/
│   │   │   └── CreateIssueDialog.jsx
│   │   │   └── IssueDetails.jsx
│   │   │   └── IssueFilters.jsx
│   │   │   └── IssueTracker.jsx
│   │   │   └── IssuesTable.jsx
│   │   │   └── ResolveIssueDialog.jsx
│   │   │   ├── createIssue/
│   │   │   │   └── IssueDetailsPanel.jsx
│   │   │   │   └── ItemAndImagesPanel.jsx
│   │   ├── layout/
│   │   │   └── CustomSnackbar.jsx
│   │   │   └── Layout.jsx
│   │   │   └── LockOverlayUI.jsx
│   │   │   └── Sidebar.jsx
│   │   │   └── TablePrimitives.jsx
│   │   │   └── WarehousePill.jsx
│   │   ├── orders/
│   │   │   └── CreateOrderDialog.jsx
│   │   │   └── DownloadWorkReportButton.jsx
│   │   │   └── OrderDetailSidebar.jsx
│   │   │   └── OrderList.jsx
│   │   │   └── OrderRow.jsx
│   │   │   └── RejectItemsDialog.jsx
│   │   │   └── ReserveItemsDialog.jsx
│   │   │   └── UploadImages.jsx
│   │   ├── processing/
│   │   │   └── LaundryProcessingList.jsx
│   │   │   └── ProcessingDetails.jsx
│   │   │   └── ProcessingDialog.jsx
│   │   │   └── ProcessingList.jsx
│   │   │   └── ProcessingPage.jsx
│   │   │   └── ProcessingRequestDetails.jsx
│   │   │   └── ProcessingRequestDialog.jsx
│   │   ├── products/
│   │   │   └── ProductDialog.jsx
│   │   │   └── ProductList.jsx
│   │   ├── stocks/
│   │   │   └── CreateInwardRequestDialog.jsx
│   │   │   └── CreateReservationDialog.jsx
│   │   │   └── ReservationDetails.jsx
│   │   │   └── StockDetails.jsx
│   │   │   └── StockInwardDialog.jsx
│   │   │   └── StockList.jsx
│   │   ├── trips/
│   │   │   └── CompleteDeliveryDialog.jsx
│   │   │   └── CompletePickupDialog.jsx
│   │   │   └── CreateTripFromRouteDialog.jsx
│   │   │   └── TripDetailsSidebar.jsx
│   │   │   └── TripManagement.jsx
│   │   │   └── TripTimelineDialog.jsx
│   │   │   └── VisitImagesDialog.jsx
│   │   │   └── VisitLevelChallanDialog.jsx
│   │   ├── users/
│   │   │   └── AddUserDialog.jsx
│   │   │   └── UserDetails.jsx
│   │   │   └── UserDialog.jsx
│   │   │   └── UserFormDialog.jsx
│   │   │   └── UserList.jsx
│   │   ├── vehicles/
│   │   │   └── VehicleManagement.jsx
│   │   ├── vendors/
│   │   │   └── ConfirmDeleteDialog.jsx
│   │   │   └── CreateLaundryVendorDialog.jsx
│   │   │   └── LaundryVendorDetails.jsx
│   │   │   └── LaundryVendorsPage.jsx
│   │   │   └── VendorDetails.jsx
│   │   │   └── VendorDialog.jsx
│   │   │   └── VendorList.jsx
│   │   ├── visits/
│   │   │   └── AddVisitDialog.jsx
│   │   ├── washRequests/
│   │   │   └── PendingItemsDashboard.jsx
│   │   │   └── WashRequestsPage.jsx
│   │   │   ├── dialogs/
│   │   │   │   └── FulfillmentDialog.jsx
│   │   │   │   └── KnockOffResultDialog.jsx
│   │   │   │   └── PopulateQuantityDialog.jsx
│   │   │   │   └── WRUnifiedDialog.jsx
│   │   │   ├── drawers/
│   │   │   │   └── EditProductQuantitiesDialog.jsx
│   │   │   │   └── LaundryProgress.jsx
│   │   │   │   └── WashFulfillmentDetails.jsx
│   │   │   │   └── WashFulfillmentDrawer.jsx
│   │   │   │   └── WashRequestDetails.jsx
│   │   │   │   └── WashRequestDrawer.jsx
│   │   │   ├── filters/
│   │   │   │   └── WashFulfillmentTabs.jsx
│   │   │   │   └── WashRequestTabs.jsx
│   │   │   ├── tables/
│   │   │   │   └── WashFulfillmentTable.jsx
│   │   │   │   └── WashRequestTable.jsx
│   └── config.jsx
│   ├── constants/
│   │   └── damageAssessment.js
│   │   └── tripItemMappings.js
│   ├── context/
│   │   └── DcidContext.jsx
│   ├── helpers/
│   │   └── HelpersPage.jsx
│   │   └── InventoryStatusUpdateModule.jsx
│   │   └── PopulateDeliveryItemsModule.jsx
│   │   └── RfidScanPage.jsx
│   │   └── formatHelperApiError.js
│   │   └── inventoryStatusUpdateConfig.js
│   │   └── rfidHelpers.js
│   ├── hooks/
│   │   └── useBilling.js
│   │   └── useDamageAssessment.js
│   │   └── useScanner.js
│   │   └── useWebSocket.js
│   └── index.css
│   └── main.jsx
│   ├── models/
│   │   └── BillingCycle.js
│   │   └── BillingPreference.js
│   │   └── Invoice.js
│   │   └── ItemDamageRequest.js
│   │   └── ScannedProducts.js
│   │   └── Scanner.js
│   ├── services/
│   │   └── agreementService.jsx
│   │   └── apiService.jsx
│   │   └── auth.jsx
│   │   └── billingService.jsx
│   │   └── customerService.jsx
│   │   └── customerUserService.jsx
│   │   └── damageAssessmentService.jsx
│   │   └── dcService.jsx
│   │   └── helperService.jsx
│   │   └── hotelService.jsx
│   │   └── inventoryService.jsx
│   │   └── inventorySummaryService.jsx
│   │   └── invoiceService.jsx
│   │   └── inwardRequestService.jsx
│   │   └── issueService.jsx
│   │   └── laundryUserService.jsx
│   │   └── laundryVendorService.jsx
│   │   └── orderService.jsx
│   │   └── processingService.jsx
│   │   └── productService.jsx
│   │   └── reservationService.jsx
│   │   └── routeService.jsx
│   │   └── scannerService.jsx
│   │   └── scannerSocketService.jsx
│   │   └── soiledService.jsx
│   │   └── tripService.jsx
│   │   └── userService.jsx
│   │   └── vendorService.jsx
│   │   └── washFulfillmentService.jsx
│   │   └── washRequestService.jsx
│   ├── state/
│   │   └── dcidStore.js
│   └── theme.js
│   ├── utils/
│   │   └──  visitItemStatus.js
│   │   └── CopyInventoryButton.jsx
│   │   └── api.jsx
│   │   └── apiHeaders.jsx
│   │   └── createColumn.js
│   │   └── dashboardData.js
│   │   └── dateUtils.js
│   │   └── inventoryUtils.js
│   │   └── pendingItemsTransformer.js
│   │   └── poolTransformers.js
│   │   └── productColorRegistry.js
│   │   └── quantityUtils.js
│   │   └── reservationTransformers.js
│   │   └── scanSound.js
│   │   └── security.js
│   │   └── statusUtils.js
│   │   └── washRequestTransformers.js
│   │   ├── workReport/
│   │   │   └── helpers.js
│   │   │   └── sheet1.js
│   │   │   └── sheet2.js
│   │   └── workReport.js
└── vite.config.js

```

---

## File Skeletons

### 📄 agent-backend/llm/orchestrator.ts
```typescript
import { ChatGoogleGenerativeAI } from "@langchain/google-genai";
import { HumanMessage, SystemMessage } from "@langchain/core/messages";
import { allTools } from "./tools.js";
import { buildIdlePrompt } from "./prompts.js";
function createModel() // [logic hidden]
function getModel() // [logic hidden]
export interface LLMToolCall {
  name: string;
  args: Record<string, unknown>;
}
export interface LLMResult {
  toolCalls: LLMToolCall[];
  rawContent: string | null;
}
/**
 * Send user text to the LLM and get tool calls back
 */
export async function orchestrate( // [logic hidden]
```

### 📄 agent-backend/llm/prompts.ts
```typescript
import routeRegistry from "../src/schema/routeRegistry.json" with { type: "json" };
function formatRoutes(): string // [logic hidden]
function formatForms(): string // [logic hidden]
/**
 * Build the system prompt for the IDLE state.
 * This is the base prompt used when no walkthrough is active.
 */
export function buildIdlePrompt(): string // [logic hidden]
```

### 📄 agent-backend/llm/tools.ts
```typescript
import { DynamicStructuredTool } from "@langchain/core/tools";
import { z } from "zod";
/**
 * Navigate to a page in the application
 */
/**
 * Start a guided walkthrough for a form
 */
/**
 * Answer a user's question
 */
/**
 * Ask the user to clarify their request
 */
```

### 📄 agent-backend/package.json
```json
{
  "name": "agent-backend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch server.ts",
    "start": "bun run server.ts",
    "test": "bun test tests/voiceHandler.acceptance.test.ts && bun test tests/walkthroughDriver.acceptance.test.ts",
    "test:voice": "bun test tests/voiceHandler.acceptance.test.ts",
    "test:driver": "bun test tests/walkthroughDriver.acceptance.test.ts"
  },
  "dependencies": {
    "@elevenlabs/elevenlabs-js": "^2.47.0",
    "@langchain/core": "^1.1.46",
    "@langchain/google-genai": "^2.1.30",
    "@types/ws": "^8.18.1",
    "dotenv": "^17.4.2",
    "langchain": "^0.3.0",
    "uuid": "^14.0.0",
    "ws": "^8.20.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
```

### 📄 agent-backend/server.ts
```typescript
import { connectionManager } from "./src/connectionManager.js";
import { routeMessage, parseMessage } from "./src/messageRouter.js";
import type { ClientData, OutgoingMessage } from "./src/types.js";
import { loadAllSchemas, getAvailableForms } from "./src/schema/loader";
import { resolve } from "path";
        type: "tool",
            type: "error",
```

### 📄 agent-backend/src/connectionManager.ts
```typescript
import type { ClientData, OutgoingMessage } from "./types";
class ConnectionManager {
  /**
   * Register a new WebSocket connection
   */
  /**
   * Unregister a WebSocket connection
   */
  /**
   * Get a specific connection by session ID
   */
  /**
   * Send a message to a specific client
   */
  /**
   * Broadcast a message to all connected clients
   */
  /**
   * Update last activity timestamp for a session
   */
  /**
   * Get total number of active connections
   */
  /**
   * Get all active session IDs
   */
  /**
   * Check if a session is connected
   */
```

### 📄 agent-backend/src/handlers/statusHandler.ts
```typescript
import type { HandlerContext, StatusMessage } from "../types.js";
export function handleStatus( // [logic hidden]
    type: "status_ack",
```

### 📄 agent-backend/src/handlers/voiceHandler.ts
```typescript
import type { VoiceMessage, HandlerContext } from "../types.js";
import { handleVoiceMessage } from "../services/voicePipeline.js";
export async function handleVoice(message: VoiceMessage, context: HandlerContext) // [logic hidden]
      type: "tool",
```

### 📄 agent-backend/src/messageRouter.ts
```typescript
import type { IncomingMessage, HandlerContext } from "./types";
import { handleVoice } from "./handlers/voiceHandler.js";
import { handleStatus } from "./handlers/statusHandler.js";
import { walkthroughDriver } from "./walkthrough/driver.js";
export function routeMessage( // [logic hidden]
        `[Router] Unknown type: "${(message as any).type}" from ${context.sessionId}`
        type: "error",
        message: `Unknown message type: "${(message as any).type}"`,
export function parseMessage(raw: string | Buffer): IncomingMessage | null // [logic hidden]
```

### 📄 agent-backend/src/schema/loader.ts
```typescript
import { readdirSync, readFileSync, existsSync } from "fs";
import { join, resolve } from "path";
export type FieldType = "text" | "date" | "select" | "autocomplete" | "toggle" | "checkbox";
export interface FieldSchema {
  key: string;
  label: string;
  type: FieldType;
  required?: boolean;
  readOnly?: boolean;
  autoFilled?: boolean;
  demoValue: unknown;
  options?: string[];
  visibleWhen?: { field: string; value: string } | Record<string, string> | null;
  conditionalOn?: { field: string; values: string[] } | null;
  explanation: string;
  tips?: string;
  pauseAfterExplain?: number;
  pauseAfterFill?: number;
  waitAfterFillMs?: number;
  commonQuestions?: Array<{ question: string; answer: string }>;
  branching?: boolean;
  exploration?: string;
  affects?: string[];
  dependsOn?: string;
  explain?: {
    full?: string;
    delta?: Record<string, string>;
  };
  autoLoad?: boolean;
  emptyMessage?: string;
}
export interface SubFormSchema {
  id: string;
  name: string;
  triggerText?: string;
  repeatable: boolean;
  autoPopulated?: boolean;
  autoPopulatedTrigger?: { field: string; description: string };
  fallbackManual?: { triggerText: string; description: string };
  demoItemCount: number;
  visibleWhen?: { field: string; value: string } | null;
  conditionalOn?: { field: string; values: string[] } | null;
  explanation?: string;
  explanationForMultiple?: string;
  copyFrom?: {
    subFormId: string;
    whenFieldEquals?: { field: string; value: string };
    checkboxLabel: string;
    copyExplanation?: string;
  };
  fields: FieldSchema[];
}
export interface FormSchema {
  id: string;
  name: string;
  mode: string;
  route: string;
  selectItem?: {
    label?: string;
    selector?: string;
  };
  openAction: {
    type: string;
    selector?: string;
    fallbackText?: string;
  };
  overview: string;
  fields: FieldSchema[];
  subForms: SubFormSchema[];
  wrapUp: string;
  prerequisites?: Array<{
    type: string;
    formId?: string;
    check?: string;
    field?: string;
    condition?: string;
  }>;
  fallback?: string;
}
export class SchemaValidationError extends Error {
/**
 * Load all schema JSON files from the forms directory.
 * Call once at server startup.
 * Throws on any invalid schema — fail fast.
 */
export function loadAllSchemas(formsDir?: string): void // [logic hidden]
/**
 * Get a schema by form ID.
 * Throws if not found — the driver should never ask for a non-existent form.
 */
export function getSchema(formId: string): FormSchema // [logic hidden]
/**
 * Get all loaded schema IDs.
 */
export function getAvailableForms(): Array<{ id: string; name: string; route: string }> // [logic hidden]
/**
 * Get context for a specific field — used by LLM for Q&A.
 * Only returns the field's data, not the entire form.
 * Token-efficient.
 */
export function getFieldContext( // [logic hidden]
function validateSchema(parsed: unknown): FormSchema // [logic hidden]
function validateField( // [logic hidden]
      `Unknown field type '${f.type}' for field '${f.key}'. Valid: ${VALID_FIELD_TYPES.join(", ")}`
function validateSubForm( // [logic hidden]
```

### 📄 agent-backend/src/services/elevenLabsSTT.ts
```typescript
export interface SttResult {
  text: string;
  languageCode: string;
  confidence: number;
}
export async function transcribe(base64Audio: string): Promise<SttResult> // [logic hidden]
  form.append("file", new Blob([audioBuffer], { type: "audio/webm" }), "audio.webm");
```

### 📄 agent-backend/src/services/elevenLabsTTS.ts
```typescript
interface TTSJob {
  text: string;
  languageCode: string;
  resolve: (val: string) => void;
  reject: (err: any) => void;
}
async function processQueue() // [logic hidden]
async function synthesizeDirect( // [logic hidden]
export function synthesize(text: string, languageCode: string): Promise<string> // [logic hidden]
```

### 📄 agent-backend/src/services/responseSender.ts
```typescript
import type { HandlerContext, OutgoingMessage } from "../types.js";
export function sendRespond( // [logic hidden]
    type: "tool",
export function sendNavigate( // [logic hidden]
  send({ type: "tool", tool: "navigate", args });
export function sendTtsAudio( // [logic hidden]
  send({ type: "tts_audio", audio: base64, messageId });
```

### 📄 agent-backend/src/services/sttService.ts
```typescript
import { transcribe, type SttResult } from "./elevenLabsSTT.js";
export async function transcribeAudio(audio: string): Promise<SttResult> // [logic hidden]
export function getRetryCount(sessionId: string): number // [logic hidden]
export function incrementRetry(sessionId: string): number // [logic hidden]
export function resetRetry(sessionId: string): void // [logic hidden]
export function getMaxRetries(): number // [logic hidden]
export type { SttResult };
```

### 📄 agent-backend/src/services/ttsService.ts
```typescript
import { synthesize } from "./elevenLabsTTS.js";
export async function synthesizeToBase64( // [logic hidden]
```

### 📄 agent-backend/src/services/voicePipeline.ts
```typescript
import type { VoiceMessage, HandlerContext } from "../types.js";
import { orchestrate } from "../../llm/orchestrator.js";
import { walkthroughDriver } from "../walkthrough/driver.js";
import * as sttService from "./sttService.js";
import * as ttsService from "./ttsService.js";
import * as responseSender from "./responseSender.js";
export async function handleVoiceMessage( // [logic hidden]
async function handleText( // [logic hidden]
async function speakAndSend( // [logic hidden]
```

### 📄 agent-backend/src/state/stateMachine.ts
```typescript
export type WalkthroughState =
export type WalkthroughEvent =
export interface TransitionPayload {
  formId?: string;
  subFormId?: string;
}
export interface WalkthroughContext {
  formId: string;
  fieldIndex: number;
  subFormId: string | null;
  subFormItemIndex: number;
  subFormFieldIndex: number;
  detourOrigin: {
    state: WalkthroughState;
    fieldIndex: number;
    subFormId: string | null;
    subFormItemIndex: number;
    subFormFieldIndex: number;
  } | null;
}
export class WalkthroughStateMachine {
```

### 📄 agent-backend/src/types.ts
```typescript
export interface VoiceMessage {
  type: "voice";
  audio?: string; // base64 encoded WebM/Opus
  text?: string;  // plain text command (MVP)
  sessionId?: string;
}
export interface StatusMessage {
  type: "status";
  event: string;
  fieldKey?: string;
  value?: string;
  reason?: string;
  tool?: string;
  newRoute?: string;
  sessionId?: string;
  [key: string]: unknown;
}
export type IncomingMessage = VoiceMessage | StatusMessage;
export interface ToolMessage {
  type: "tool";
  tool: string;
  args: Record<string, unknown>;
}
export interface TtsAudioMessage {
  type: "tts_audio";
  audio: string; // base64 encoded MP3
  messageId: string;
}
export interface StatusAckMessage {
  type: "status_ack";
  event: string;
}
export interface ErrorMessage {
  type: "error";
  message: string;
  code?: string;
}
export type OutgoingMessage =
export interface ClientData {
  sessionId: string;
  connectedAt: number;
  lastActivityAt: number;
}
export interface HandlerContext {
  sessionId: string;
  send: (message: OutgoingMessage) => void;
}
```

### 📄 agent-backend/src/walkthrough/driver.ts
```typescript
import {
  type WalkthroughSession,
  type FieldSchema,
  type SubFormSchema,
  type FormSchema,
import { StatusAwaiter, CancellationError } from "./statusAwaiter.js";
import { ToolMessenger } from "./toolMessenger.js";
import { NarrationService } from "./narrationService.js";
import type { OutgoingMessage } from "../types.js";
class FieldSkipError extends Error {
class WalkthroughAbortError extends Error {
class WalkthroughDriver {
        type: "tool",
      const { getAvailableForms } = await import("./sessionManager.js");
        type: "tool",
            type: "tool",
      type: "tool",
      const { WalkthroughPlanner } = await import("./planner.js");
                    { fieldKey: field.key, label: field.label, type: field.type, value: String(val) },
    const { key, label, type, explanation, demoValue, readOnly, autoFilled } = field;
          { fieldKey: key, label, type, value: String(demoValue) },
                  type: field.type,
                  type: field.type,
    this.send(session.sessionId, { type: "tool", tool, args });
```

### 📄 agent-backend/src/walkthrough/narrationService.ts
```typescript
import { connectionManager } from "../connectionManager.js";
import type { StatusAwaiter } from "./statusAwaiter.js";
import type { WalkthroughSession } from "./sessionManager.js";
export class NarrationService {
      const { synthesize } = await import("../services/elevenLabsTTS.js");
        type: "tts_audio",
```

### 📄 agent-backend/src/walkthrough/planner.ts
```typescript
import type { FormSchema, FieldSchema } from "../schema/loader.js";
import { TEMPLATES } from "./narrationTemplates.js";
export interface PlannerStep {
  cmd: 'go_to_field' | 'explain_field' | 'fill_field' | 'wait';
  key?: string;
  text?: string;
  value?: unknown;
  ms?: number;
}
export class WalkthroughPlanner {
```

### 📄 agent-backend/src/walkthrough/sessionManager.ts
```typescript
import { WalkthroughStateMachine } from "../state/stateMachine.js";
import {
  type FormSchema,
  type FieldSchema,
  type SubFormSchema,
export interface PendingStatus {
  expectedEvent: string;
  matcher?: (data: any) => boolean;
  resolve: (data: any) => void;
  reject: (reason: any) => void;
  timer: ReturnType<typeof setTimeout>;
}
export interface WalkthroughSession {
  sessionId: string;
  formId: string;
  schema: FormSchema;
  stateMachine: WalkthroughStateMachine;
  filledValues: Map<string, unknown>;
  cancelled: boolean;
  errorCount: number;
  pendingStatus: PendingStatus | null;
  isRegistered: boolean;
}
export class SessionManager {
export { getSchema, getAvailableForms, type FormSchema, type FieldSchema, type SubFormSchema };
```

### 📄 agent-backend/src/walkthrough/statusAwaiter.ts
```typescript
import type { WalkthroughSession, PendingStatus } from "./sessionManager.js";
export class CancellationError extends Error {
export class StatusAwaiter {
```

### 📄 agent-backend/src/walkthrough/toolMessenger.ts
```typescript
import { connectionManager } from "../connectionManager.js";
import type { OutgoingMessage } from "../types.js";
export class ToolMessenger {
```

### 📄 agent-backend/test-client.ts
```typescript
      type: "voice",
```

### 📄 agent-backend/test-state-machine.ts
```typescript
import { WalkthroughStateMachine } from "./src/state/stateMachine";
```

### 📄 agent-backend/tests/voiceHandler.acceptance.test.ts
```typescript
import { describe, test, expect, mock, beforeEach } from "bun:test";
const { handleVoice } = await import("../src/handlers/voiceHandler.js");
const sttService = await import("../src/services/sttService.js");
function createCtx(overrides?: Record<string, unknown>) // [logic hidden]
function resetAllMocks() // [logic hidden]
function makeRetryMock() // [logic hidden]
      await handleVoice({ type: "voice", text: "test:createOrder" }, ctx as any);
      await handleVoice({ type: "voice", text: "test:myForm" }, ctx as any);
      await handleVoice({ type: "voice", text: "test:  createHotel  " }, ctx as any);
      await handleVoice({ type: "voice", text: "navigate to orders" }, ctx as any);
      await handleVoice({ type: "voice", text: "help" }, ctx as any);
      await handleVoice({ type: "voice", audio: "base64audio" }, ctx as any);
      await handleVoice({ type: "voice" }, ctx as any);
      await handleVoice({ type: "voice", audio: "base64audio" }, ctx as any);
      await handleVoice({ type: "voice", audio: "a1" }, ctx1 as any);
      await handleVoice({ type: "voice", audio: "a2" }, ctx2 as any);
      await handleVoice({ type: "voice", audio: "a1" }, ctx1 as any);
      await handleVoice({ type: "voice", audio: "a2" }, ctx2 as any);
      await handleVoice({ type: "voice", audio: "a3" }, ctx3 as any);
      await handleVoice({ type: "voice", audio: "a1" }, ctx1 as any);
      await handleVoice({ type: "voice", audio: "a2" }, ctx2 as any);
      await handleVoice({ type: "voice", audio: "a3" }, ctx3 as any);
      await handleVoice({ type: "voice", text: "go to orders" }, ctx as any);
      await handleVoice({ type: "voice", text: "show me orders" }, ctx as any);
      await handleVoice({ type: "voice", text: "where are orders" }, ctx as any);
      await handleVoice({ type: "voice", text: "show me the form" }, ctx as any);
      await handleVoice({ type: "voice", text: "do something weird" }, ctx as any);
      await handleVoice({ type: "voice", text: "test?" }, ctx as any);
      await handleVoice({ type: "voice", text: "test?" }, ctx as any);
      await handleVoice({ type: "voice", audio: "base64audio" }, ctx as any);
```

### 📄 agent-backend/tests/walkthroughDriver.acceptance.test.ts
```typescript
import { describe, test, expect, mock, beforeEach } from "bun:test";
  openAction: { type: "click", fallbackText: "Create Order" },
      type: "text" as const,
      type: "select" as const,
const { walkthroughDriver } = await import("../src/walkthrough/driver.js");
function delay(ms: number) // [logic hidden]
function sentMessages(sessionId: string): any[] // [logic hidden]
function resetAllMocks() // [logic hidden]
/**
 * Drive a walkthrough session through the initial setup:
 * navigate → dialog_opened → form_registered
 * Returns after overview is sent.
 */
async function driveToOverview(sid: string) // [logic hidden]
```

### 📄 agent-backend/tsconfig.json
```json
{
  "compilerOptions": {
    "target": "ESNext",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "types": ["bun-types"],
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true,
    "declaration": true,
    "declarationMap": true,
    "sourceMap": true,
    "outDir": "./dist",
    "rootDir": ".",
    "baseUrl": ".",
    "paths": {
      "@/*": ["./*"]
    }
  },
  "include": ["**/*.ts"],
  "exclude": ["node_modules", "dist"]
}
```

### 📄 agent-backend/utils/keywordMatcher.ts
```typescript
export type KeywordAction = "resume" | "pause" | "cancel" | null;
/**
 * Check if the transcribed text matches a known keyword command.
 * Returns the action if matched, null otherwise.
 * Only matches if the ENTIRE text is a keyword (or very close to one).
 */
export function matchKeyword(text: string): KeywordAction // [logic hidden]
```

### 📄 agent-backend/voice/stt.ts
```typescript
export interface STTResult {
  success: boolean;
  text: string;
  languageCode?: string;
  error?: string;
}
/**
 * Send audio to ElevenLabs STT and get transcribed text
 */
export async function transcribeAudio(audioBuffer: Buffer): Promise<STTResult> // [logic hidden]
    const blob = new Blob([arrayBuffer], { type: "audio/webm;codecs=opus" });
```

### 📄 agent-backend/voice/tts.ts
```typescript
export interface TTSResult {
  success: boolean;
  audioBuffer: Buffer | null;
  error?: string;
}
/**
 * Generate speech audio from text using ElevenLabs TTS
 */
export async function generateSpeech(text: string): Promise<TTSResult> // [logic hidden]
```

### 📄 eslint.config.js
```javascript
import js from '@eslint/js'
import globals from 'globals'
import reactHooks from 'eslint-plugin-react-hooks'
import reactRefresh from 'eslint-plugin-react-refresh'
```

### 📄 package.json
```json
{
  "name": "admin-panel-linengrass",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "lint": "eslint .",
    "preview": "vite preview"
  },
  "dependencies": {
    "@emotion/react": "^11.14.0",
    "@emotion/styled": "^11.14.0",
    "@mui/icons-material": "^7.0.2",
    "@mui/lab": "^7.0.1-beta.18",
    "@mui/material": "^7.0.2",
    "@mui/x-charts": "^8.12.0",
    "@mui/x-date-pickers": "^8.2.0",
    "@stomp/stompjs": "^7.3.0",
    "@tanstack/react-query": "^5.90.16",
    "ajv": "^8.17.1",
    "axios": "^1.9.0",
    "date-fns": "^4.1.0",
    "exceljs": "^4.4.0",
    "file-saver": "^2.0.5",
    "formik": "^2.4.6",
    "lodash.debounce": "^4.0.8",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "react-hook-form": "^7.70.0",
    "react-router-dom": "^7.5.3",
    "react-toastify": "^11.0.5",
    "react-window": "^1.8.11",
    "sockjs-client": "^1.6.1",
    "xlsx": "^0.18.5",
    "yet-another-react-lightbox": "^3.25.0",
    "yup": "^1.6.1"
  },
  "devDependencies": {
    "@eslint/js": "^9.25.0",
    "@types/react": "^19.1.2",
    "@types/react-dom": "^19.1.2",
    "@vitejs/plugin-react": "^4.4.1",
    "eslint": "^9.25.0",
    "eslint-plugin-react-hooks": "^5.2.0",
    "eslint-plugin-react-refresh": "^0.4.19",
    "globals": "^16.0.0",
    "javascript-obfuscator": "^5.4.1",
    "vite": "^6.3.5",
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
```

### 📄 src/App.jsx
```javascript
import React, { Suspense } from 'react';
import { BrowserRouter as Router, Routes, Route, Navigate, Outlet, useNavigate } from 'react-router-dom';
import { ThemeProvider } from '@mui/material/styles';
import { LocalizationProvider } from '@mui/x-date-pickers';
import { AdapterDateFns } from '@mui/x-date-pickers/AdapterDateFns';
import { enGB } from 'date-fns/locale';
import { ToastContainer } from 'react-toastify';
import CircularProgress from '@mui/material/CircularProgress';
import Box from '@mui/material/Box';
import 'react-toastify/dist/ReactToastify.css';
import theme from './theme';
import './App.css';
import Layout from './components/layout/Layout';
import { AgentBridgeProvider, AgentErrorBoundary } from './agent/AgentBridge';
import AgentOverlay from './agent/AgentOverlay';
import NavigationHandler from './agent/NavigationHandler';
import WalkthroughHandler from './agent/WalkthroughHandler';
const Login = React.lazy(() => import('./components/auth/Login'));
const HotelList = React.lazy(() => import('./components/hotels/HotelList'));
const ProductList = React.lazy(() => import('./components/products/ProductList'));
const OrderList = React.lazy(() => import('./components/orders/OrderList'));
const TripManagement = React.lazy(() => import('./components/trips/TripManagement'));
const VendorList = React.lazy(() => import('./components/vendors/VendorList'));
const ProcessingList = React.lazy(() => import('./components/processing/ProcessingList'));
const InvoicesPage = React.lazy(() => import('./components/invoices/InvoicesPage'));
const UserList = React.lazy(() => import('./components/users/UserList'));
const CustomersPage = React.lazy(() => import('./components/customers/CustomersPage'));
const InvoiceGenerator = React.lazy(() => import('./components/invoices/InvoiceGenerator'));
const LaundryVendorsPage = React.lazy(() => import('./components/vendors/LaundryVendorsPage'));
const VehicleManagement = React.lazy(() => import('./components/vehicles/VehicleManagement'));
const WashRequestsPage = React.lazy(() => import('./components/washRequests/WashRequestsPage'));
const InventoryPage = React.lazy(() => import('./components/inventory/InventoryPage'));
const InventorySummaryPage = React.lazy(() => import('./components/inventorySummary/InventorySummaryPage'));
const InventoryItemReservationPage = React.lazy(() => import('./components/inventoryItemReservation/InventoryItemReservtionPage'));
const RouteManagementPage = React.lazy(() => import('./components/RouteManagement/RouteManagementPage'));
const Dashboard = React.lazy(() => import('./components/dashboard/Dashboard'));
const IssueTracker = React.lazy(() => import('./components/issueTracker/IssueTracker'));
const InventoryPoolPage = React.lazy(() => import('./components/inventory-pool/InventoryPoolPage'));
const InventoryPoolTable = React.lazy(() => import('./components/inventory-pool/InventoryPoolTable'));
const InventoryReservation = React.lazy(() => import('./components/InventoryReservation/InventoryReservation'));
const DamageAssessmentDashboard = React.lazy(() => import('./components/damageAssessment/DamageAssessmentDashboard'));
const BillManagementPage = React.lazy(() => import('./components/billing/BillManagementPage'));
const BillingCycle = React.lazy(() => import('./components/billing/BillingCycle'));
const BillingPreference = React.lazy(() => import('./components/billing/BillingPreference'));
const HelpersPage = React.lazy(() => import('./helpers/HelpersPage'));
function App() // [logic hidden]
```

### 📄 src/agent/AgentBridge.jsx
```javascript
import { createContext, useContext, useState, useEffect, useCallback, useRef } from "react";
import { AudioQueue } from "./AudioQueue";
import { routeIncomingMessage } from "./messageRouter";
import { connectWs, disconnectWs, sendMessage as wsSendMessage, onStatusChange } from "./wsConnection";
import { STATUS, TIMING } from "./protocol";
export function AgentBridgeProvider({ children }) // [logic hidden]
        sendMessage({ type: "voice", audio: base64 });
```

### 📄 src/agent/AgentErrorBoundary.jsx
```javascript
import React from "react";
import { Alert, Box, Button } from "@mui/material";
export class AgentErrorBoundary extends React.Component {
```

### 📄 src/agent/AgentOverlay.jsx
```javascript
import { useState, useRef, useCallback, useEffect } from "react";
import { useAgent } from "./AgentBridge";
import {
import MicIcon from "@mui/icons-material/Mic";
import MicOffIcon from "@mui/icons-material/MicOff";
import KeyboardArrowDownIcon from "@mui/icons-material/KeyboardArrowDown";
import CloseIcon from "@mui/icons-material/Close";
import { STATUS, TIMING } from "./protocol";
import "./spotlight.css";
export default function AgentOverlay() // [logic hidden]
        const blob = new Blob(chunksRef.current, { type: "audio/webm;codecs=opus" });
      sendMessage({ type: "voice", text: text.trim() });
```

### 📄 src/agent/AudioQueue.js
```javascript
import { sendStatus } from "./wsConnection";
import { STATUS_EVENTS } from "./protocol";
export class AudioQueue {
```

### 📄 src/agent/NavigationHandler.jsx
```javascript
import { useEffect, useRef } from "react";
import { useNavigate, useLocation } from "react-router-dom";
import { useAgent } from "./AgentBridge";
import { STATUS_EVENTS } from "./protocol";
import { sendStatus } from "./wsConnection";
export default function NavigationHandler() // [logic hidden]
```

### 📄 src/agent/WalkthroughHandler.jsx
```javascript
import { useEffect, useRef } from "react";
import { useAgent } from "./AgentBridge";
import { agentFormRegistry } from "./agentFormRegistry";
import { executeTool } from "./toolRegistry";
import { TOOL_TYPES } from "./protocol";
import { SpotlightManager } from "./SpotlightManager";
import { wait } from "./utils";
import "./toolHandlers/index";
export default function WalkthroughHandler() // [logic hidden]
    const { type } = pendingTool;
    if (type === TOOL_TYPES.PAUSE_WALKTHROUGH) {
      isPausedRef.current = true;
      setIsPaused(true);
      clearPendingTool();
      return;
    }
    if (type === TOOL_TYPES.RESUME_WALKTHROUGH) {
      isPausedRef.current = false;
      setIsPaused(false);
      clearPendingTool();
      drain();
      return;
    }
    if (type === TOOL_TYPES.CANCEL_WALKTHROUGH) {
      queueRef.current = [];
      executingRef.current = false;
      isPausedRef.current = false;
      setIsPaused(false);
      isDetourActiveRef.current = false;
      SpotlightManager.clearSpotlight();
      stopAudio();
      clearMessages();
      executeTool(TOOL_TYPES.CLEAR_ALL_FIELDS, {}, {
        send: sendRef.current,
        add: addRef.current,
        formId: activeFormIdRef.current,
        setActiveFormId: (id) => { activeFormIdRef.current = id; }
      });
      clearPendingTool();
      return;
    }
    if (type === "detour_start") {
      isDetourActiveRef.current = true;
      clearPendingTool();
      return;
    }
    if (type === "detour_end") {
      isDetourActiveRef.current = false;
      clearPendingTool();
      drain();
      return;
    }
  function drain() // [logic hidden]
  async function execute(tool) // [logic hidden]
    const { type, args } = tool;
      await executeTool(type, args, context);
```

### 📄 src/agent/agentFormRegistry.js
```javascript
class AgentFormRegistry {
```

### 📄 src/agent/fieldFiller/buttonHelper.js
```javascript
import { TIMING } from "../protocol";
import { findCheckboxInput } from "../utils";
export async function clickButton(text, timeout = TIMING.BUTTON_TIMEOUT_MS) // [logic hidden]
export function clickCheckbox(labelText) // [logic hidden]
```

### 📄 src/agent/fieldFiller/dialogHelper.js
```javascript
export function closeDialog() // [logic hidden]
```

### 📄 src/agent/fieldFiller/fillAutocomplete.js
```javascript
import { setNativeValue } from "./nativeSetValue";
import { TIMING } from "../protocol";
export async function doFillAutocomplete(element, value) // [logic hidden]
async function pollForAutocompleteOption(searchValue) // [logic hidden]
```

### 📄 src/agent/fieldFiller/fillSelect.js
```javascript
export async function fillSelectField(element, value) // [logic hidden]
```

### 📄 src/agent/fieldFiller/fillText.js
```javascript
import { setNativeValue } from "./nativeSetValue";
export async function fillTextField(element, value) // [logic hidden]
export function fillDateField(element, value) // [logic hidden]
```

### 📄 src/agent/fieldFiller/fillToggle.js
```javascript
import { findCheckboxInput, isChecked, isTruthyValue } from "../utils";
export function fillToggleField(element, value) // [logic hidden]
export function fillCheckboxField(element, value) // [logic hidden]
```

### 📄 src/agent/fieldFiller/finder.js
```javascript
export function findField(fieldKey, label, subFormId, itemIndex, container = document) // [logic hidden]
function findFieldByLabel(label, subFormId, itemIndex, container) // [logic hidden]
function findSubFormField(root, label, subFormId, itemIndex) // [logic hidden]
function scrollToElement(element) // [logic hidden]
```

### 📄 src/agent/fieldFiller/index.js
```javascript
import { findField as findFieldInternal } from "./finder";
import { fillTextField, fillDateField } from "./fillText";
import { fillSelectField } from "./fillSelect";
import { doFillAutocomplete } from "./fillAutocomplete";
import { fillToggleField, fillCheckboxField } from "./fillToggle";
import { setNativeValue } from "./nativeSetValue";
export function findField(fieldKey, label, subFormId, itemIndex, container = document) // [logic hidden]
export async function fillField(fieldKey, label, type, value, subFormId, itemIndex, container = document) {
  const element = findField(fieldKey, label, subFormId, itemIndex, container);
  if (!element) {
    return { success: false, reason: `Field not found: ${fieldKey || label}` };
  }
  switch (type) {
    case "text":
      return fillTextField(element, value);
    case "date":
      return fillDateField(element, value);
    case "select":
      return await fillSelectField(element, value);
    case "autocomplete":
      return { success: false, reason: "Use fillAutocompleteField for autocomplete fields" };
    case "toggle":
      return fillToggleField(element, value);
    case "checkbox":
      return fillCheckboxField(element, value);
    default:
      return { success: false, reason: `Unknown field type: ${type}` };
  }
}
export async function fillAutocompleteField(fieldKey, label, value, subFormId, itemIndex, container = document) // [logic hidden]
export function clearAllFields(container) // [logic hidden]
    const type = el.getAttribute("data-agent-filled");
    if (type === "text" || type === "date") {
      const input = el.querySelector("input");
      if (input) {
        setNativeValue(input, "");
        input.dispatchEvent(new Event("input", { bubbles: true }));
        input.dispatchEvent(new Event("change", { bubbles: true }));
        cleared++;
      }
    } else if (type === "select") {
      const select = el.querySelector('[role="combobox"]');
      if (select) {
        const nativeInput = el.querySelector(".MuiSelect-nativeInput") || el.querySelector("input");
        if (nativeInput) {
          setNativeValue(nativeInput, "");
          nativeInput.dispatchEvent(new Event("input", { bubbles: true }));
          nativeInput.dispatchEvent(new Event("change", { bubbles: true }));
          const inputKey = Object.keys(nativeInput).find((k) => k.startsWith("__reactProps"));
          if (inputKey && nativeInput[inputKey]?.onChange) {
            nativeInput[inputKey].onChange({ target: { value: "", name: nativeInput.name } });
          }
        }
        const wrapper = select.closest(".MuiInputBase-root") || select;
        const wrapperKey = Object.keys(wrapper).find((k) => k.startsWith("__reactProps"));
        if (wrapperKey && wrapper[wrapperKey]?.onChange) {
          wrapper[wrapperKey].onChange({ target: { value: "", name: nativeInput?.name || select.name } });
        }
        const selectKey = Object.keys(select).find((k) => k.startsWith("__reactProps"));
        if (selectKey && select[selectKey]?.onChange) {
          select[selectKey].onChange({ target: { value: "", name: nativeInput?.name || select.name } });
        }
        const display = el.querySelector(".MuiSelect-select");
        if (display) {
          display.textContent = "";
        }
        cleared++;
      }
    } else if (type === "autocomplete") {
      const input = el.querySelector("input");
      if (input) {
        setNativeValue(input, "");
        input.dispatchEvent(new Event("input", { bubbles: true }));
        const reactKey = Object.keys(input).find((k) =>
          k.startsWith("__reactProps")
        );
        if (reactKey && input[reactKey]?.onChange) {
          input[reactKey].onChange({ target: { value: "" } });
        }
        cleared++;
      }
    } else if (type === "toggle") {
      const switchInput = el.querySelector(
        'input[type="checkbox"], [role="switch"]'
      );
      if (switchInput) {
        const original = el.getAttribute("data-agent-original") === "true";
        const current =
          switchInput.checked ||
          switchInput.getAttribute("aria-checked") === "true";
        if (current !== original) {
          switchInput.click();
          cleared++;
        }
      }
    }
```

### 📄 src/agent/fieldFiller/nativeSetValue.js
```javascript
export function setNativeValue(element, value) // [logic hidden]
```

### 📄 src/agent/formExecutor.js
```javascript
import { agentFormRegistry } from "./agentFormRegistry";
export function findRegistryField(form, fieldKey) // [logic hidden]
export async function fillRegistryField(formId, fieldKey, value, itemIndex) // [logic hidden]
export function searchRegistryField(formId, fieldKey, query) // [logic hidden]
export async function waitForRegistryOption(formId, fieldKey, searchText, timeout = 6000) // [logic hidden]
export function getRegistryFieldElement(formId, fieldKey, itemIndex) // [logic hidden]
export async function clearRegistryForm(formId) // [logic hidden]
export async function addRegistryItem(formId, subFormId) // [logic hidden]
```

### 📄 src/agent/messageRouter.js
```javascript
import { TOOL_TYPES, STATUS_EVENTS, MESSAGE_TYPES } from "./protocol";
      setPendingTool({ type: TOOL_TYPES.START_WALKTHROUGH, formId, uuid: crypto.randomUUID() });
    setPendingTool({ type: TOOL_TYPES.RESUME_WALKTHROUGH, uuid: crypto.randomUUID() });
    setPendingTool({ type: TOOL_TYPES.PAUSE_WALKTHROUGH, uuid: crypto.randomUUID() });
    setPendingTool({ type: TOOL_TYPES.CANCEL_WALKTHROUGH, uuid: crypto.randomUUID() });
export function routeIncomingMessage( // [logic hidden]
          setPendingTool({ type: message.tool, args: message.args, uuid: crypto.randomUUID() });
        console.warn(`[AgentBridge] Unknown message type: ${message.type}`);
```

### 📄 src/agent/toolHandlers/actionTools.js
```javascript
import { registerTool } from "../toolRegistry";
import { TOOL_TYPES, STATUS_EVENTS, TIMING } from "../protocol";
import * as filler from "../fieldFiller/index";
import { agentFormRegistry } from "../agentFormRegistry";
import { sendStatus, sendError } from "../wsConnection";
import { wait } from "../utils";
    const { addRegistryItem } = await import("../formExecutor");
```

### 📄 src/agent/toolHandlers/begin_walkthrough.js
```javascript
import { registerTool } from "../toolRegistry";
import { TOOL_TYPES, STATUS_EVENTS } from "../protocol";
import { sendStatus } from "../wsConnection";
```

### 📄 src/agent/toolHandlers/fieldTools.js
```javascript
import { registerTool } from "../toolRegistry";
import { TOOL_TYPES, STATUS_EVENTS } from "../protocol";
import * as filler from "../fieldFiller/index";
import { agentFormRegistry } from "../agentFormRegistry";
import { fillRegistryField, findRegistryField } from "../formExecutor";
import { SpotlightManager } from "../SpotlightManager";
import { sendStatus, sendError } from "../wsConnection";
function getActiveContainer() // [logic hidden]
async function fillViaDOM(fieldKey, label, type, value, subFormId, itemIndex) {
  const container = getActiveContainer();
  if (type === "autocomplete") {
    const result = await filler.fillAutocompleteField(fieldKey, label, value, subFormId, itemIndex, container);
    if (!result.success) throw new Error(result.reason);
  } else {
    const result = await filler.fillField(fieldKey, label, type, value, subFormId, itemIndex, container);
    if (!result.success) throw new Error(result.reason);
  }
}
  const { fieldKey, label, type, value, subFormId, itemIndex } = args;
        if (type === "autocomplete") {
           const { searchRegistryField, waitForRegistryOption } = await import("../formExecutor");
           searchRegistryField(formId, fieldKey, value);
           const match = await waitForRegistryOption(formId, fieldKey, value);
           if (!match) throw new Error(`No autocomplete results for: ${value}`);
           const result = await fillRegistryField(formId, fieldKey, match, itemIndex);
           if (!result.success) throw new Error(result.reason);
        } else {
           const result = await fillRegistryField(formId, fieldKey, value, itemIndex);
           if (!result.success) throw new Error(result.reason);
        }
        await fillViaDOM(fieldKey, label, type, value, subFormId, itemIndex);
      await fillViaDOM(fieldKey, label, type, value, subFormId, itemIndex);
      const { clearRegistryForm } = await import("../formExecutor");
```

### 📄 src/agent/toolHandlers/index.js
```javascript
import "./begin_walkthrough";
import "./fieldTools";
import "./actionTools";
```

### 📄 src/agent/toolRegistry.js
```javascript
export function registerTool(name, handler) // [logic hidden]
export async function executeTool(name, args, context) // [logic hidden]
```

### 📄 src/agent/useAgentForm.js
```javascript
import { useEffect, useRef } from "react";
import { agentFormRegistry } from "./agentFormRegistry";
import { sendStatus } from "./wsConnection";
import { STATUS_EVENTS } from "./protocol";
export function useAgentForm(formId, formApi) // [logic hidden]
```

### 📄 src/agent/utils.js
```javascript
export function wait(ms) // [logic hidden]
export function findCheckboxInput(element) // [logic hidden]
export function isChecked(element) // [logic hidden]
export function isTruthyValue(value) // [logic hidden]
```

### 📄 src/agent/wsConnection.js
```javascript
import { MESSAGE_TYPES, STATUS_EVENTS, TIMING } from "./protocol";
export function onStatusChange(cb) // [logic hidden]
function emitStatus(status) // [logic hidden]
export function connectWs(url, { onOpen, onMessage, onClose, onError }) // [logic hidden]
export function disconnectWs() // [logic hidden]
export function sendMessage(message) // [logic hidden]
export function sendStatus(event, payload = {}) // [logic hidden]
    type: MESSAGE_TYPES.STATUS,
export function sendError(tool, reason, extra = {}) // [logic hidden]
```

### 📄 src/components/InventoryReservation/CreateReservationDialog.jsx
```javascript
import { useState, useEffect, useCallback } from "react";
import {
import { inventoryService } from "../../services/inventoryService";
import { customerService } from "../../services/customerService";
import { agreementService } from "../../services/agreementService";
function CreateReservationDialog( // [logic hidden]
```

### 📄 src/components/InventoryReservation/CreateTransactionDialog.jsx
```javascript
import { useState} from "react";
import {
function CreateTransactionDialog( // [logic hidden]
```

### 📄 src/components/InventoryReservation/CustomerTransactionDrawer.jsx
```javascript
import { useState, useEffect, useCallback } from "react";
import {
import CustomDrawer from "../common/CustomDrawer";
import DataTable from "../common/tables/DataTable";
import { inventoryService } from "../../services/inventoryService";
import { DATE_TIME, formatCustomDate } from "../../utils/dateUtils";
import CreateTransactionDialog from "./CreateTransactionDialog";
import AddIcon from "@mui/icons-material/Add";
function CustomerTransactionDrawer({ open, onClose, reservation, onRefresh }) // [logic hidden]
      type: "shortText",
      type: "shortText",
    { field: "totalQuantity", headerName: "Quantity", type: "smallNumber" },
      type: "shortText",
      type: "text",
      type: "shortText",
```

### 📄 src/components/InventoryReservation/InventoryReservation.jsx
```javascript
import { useState, useEffect, useCallback } from "react";
import {
import AddIcon from "@mui/icons-material/Add";
import { inventoryService } from "../../services/inventoryService";
import CreateReservationDialog from "./CreateReservationDialog";
import CustomSnackbar from "../layout/CustomSnackbar";
import { reservationTransformers } from "../../utils/reservationTransformers";
import { transformPools } from "../../utils/poolTransformers";
import CustomerTransactionDrawer from "./CustomerTransactionDrawer";
import DataTable from "../common/tables/DataTable";
function InventoryReservation() // [logic hidden]
    { field: "id", headerName: "ID", type: "id" },
      type: "shortText",
      type: "smallNumber",
      type: "smallNumber",
      type: "smallNumber",
      type: "smallNumber",
      type: "actions",
      type: "shortText",
      type: "smallNumber",
      type: "smallNumber",
      type: "smallNumber",
      type: "smallNumber",
      type: "smallNumber",
```

### 📄 src/components/RouteManagement/AssignCustomersDialog.jsx
```javascript
import { useEffect, useState, useMemo } from "react";
import {
import { alpha } from '@mui/material/styles';
import { 
import { routeService } from "../../services/routeService";
import { laundryVendorService } from "../../services/laundryVendorService";
import CustomSnackbar from "../layout/CustomSnackbar";
function AssignPointsDialog({ open, onClose, route, onAssigned }) // [logic hidden]
```

### 📄 src/components/RouteManagement/CreateRouteDialog.jsx
```javascript
import React, { useEffect, useMemo, useState } from 'react';
import {
import { Route as RouteIcon } from '@mui/icons-material';
import { routeService } from '../../services/routeService';
import { inventoryService } from '../../services/inventoryService.jsx';
import { useDcid } from '../../context/DcidContext.jsx';
function CreateRouteDialog({ open, onClose, onCreated }) // [logic hidden]
    async function fetchWh() // [logic hidden]
```

### 📄 src/components/RouteManagement/RouteDrawer.jsx
```javascript
import React from 'react';
import {
import CloseIcon from '@mui/icons-material/Close';
import GroupIcon from '@mui/icons-material/Group';
import EmailIcon from '@mui/icons-material/Email';
import PhoneIcon from '@mui/icons-material/Phone';
import RouteIcon from '@mui/icons-material/Route';
import BusinessIcon from '@mui/icons-material/Business';
function RouteDrawer({ open, onClose, route }) // [logic hidden]
```

### 📄 src/components/RouteManagement/RouteList.jsx
```javascript
import React, { useMemo, useState } from 'react';
import {
import AssignmentIndIcon from '@mui/icons-material/AssignmentInd';
import VisibilityIcon from '@mui/icons-material/Visibility';
function AssignedPointsCell({ points = [] }) // [logic hidden]
function RouteList({ routes, onAssign, onViewDetails }) // [logic hidden]
```

### 📄 src/components/RouteManagement/RouteManagementPage.jsx
```javascript
import React, { useState, useEffect, useMemo } from "react";
import { 
import { Add as AddIcon, Search as SearchIcon } from '@mui/icons-material';
import CreateRouteDialog from "./CreateRouteDialog";
import AssignPointsDialog from "./AssignCustomersDialog";
import RouteList from "./RouteList";
import RouteDrawer from "./RouteDrawer";
import CustomSnackbar from '../layout/CustomSnackbar';
import { routeService } from "../../services/routeService";
function RouteManagementPage() // [logic hidden]
```

### 📄 src/components/Scanner/ColumnScanHeaderAction.jsx
```javascript
import React, { useState, useCallback } from "react";
import { Box, Typography, Tooltip } from "@mui/material";
import ScannerPicker from "./ScannerPicker";
import ScannedIdsIndicator from "./ScannedIdsIndicator";
```

### 📄 src/components/Scanner/QuantityScanInput.jsx
```javascript
import React, { useState } from "react";
import { Box, TextField, Typography, Tooltip } from "@mui/material";
import ScannerPicker from "./ScannerPicker";
import ScannedIdsIndicator from "./ScannedIdsIndicator";
```

### 📄 src/components/Scanner/ScannedIdsIndicator.jsx
```javascript
import React, { useState } from 'react';
import { Popover, Typography, Box, Chip } from '@mui/material';
```

### 📄 src/components/Scanner/ScannerController.jsx
```javascript
import { scannerService } from "../../services/scannerService";
import { scannerSocketService } from "../../services/scannerSocketService";
class ScannerController {
```

### 📄 src/components/Scanner/ScannerHeader.jsx
```javascript
import React, { useEffect, useRef } from "react";
import { Box, Stack, Typography, Button } from "@mui/material";
import { scannerController } from "./ScannerController";
```

### 📄 src/components/Scanner/ScannerPicker.jsx
```javascript
import { useState, useEffect, useRef, useCallback } from "react";
import {
import WifiIcon from "@mui/icons-material/Wifi";
import DeleteOutlineIcon from "@mui/icons-material/DeleteOutline";
import AddIcon from "@mui/icons-material/Add";
import { scannerService } from "../../services/scannerService";
import { scannerController } from "./ScannerController";
          type: "INFO",
```

### 📄 src/components/auth/Login.jsx
```javascript
import React, { useEffect, useState } from "react";
import {
import EmailRoundedIcon from "@mui/icons-material/EmailRounded";
import LockRoundedIcon from "@mui/icons-material/LockRounded";
import VisibilityOffRoundedIcon from "@mui/icons-material/VisibilityOffRounded";
import VisibilityRoundedIcon from "@mui/icons-material/VisibilityRounded";
import ArrowForwardRoundedIcon from "@mui/icons-material/ArrowForwardRounded";
import { useNavigate } from "react-router-dom";
import { login } from "../../services/auth";
function Login() // [logic hidden]
```

### 📄 src/components/billing/BillManagementPage.jsx
```javascript
import { Container } from "@mui/material";
import { useLocation, Outlet } from "react-router-dom";
import TabsHeader from "../common/TabsHeader";
function BillManagementPage() // [logic hidden]
```

### 📄 src/components/billing/BillingCycle.jsx
```javascript
import React, { useState } from "react";
import {
import {
import DataTable from "../common/tables/DataTable";
import LoaderScreen from "../dashboard/LoaderScreen";
import StatusChip from "../common/StatusChip";
import GreenButton from "../common/GreenButton";
import { useBillingCycles, useBillingCycleAnnexureUrl } from "../../hooks/useBilling";
import { useQueryClient } from "@tanstack/react-query";
import { customerService } from "../../services/customerService";
import { formatCustomDate, generateMonthOptions, getMonthRange } from "../../utils/dateUtils";
import AutoFixHighIcon from "@mui/icons-material/AutoFixHigh";
import BillingCycleSidebar from "./drawers/BillingCycleSidebar";
import ProcessOrdersDialog from "./dialogs/ProcessOrdersDialog";
import CustomSnackbar from "../layout/CustomSnackbar";
function BillingCycle() // [logic hidden]
```

### 📄 src/components/billing/BillingPreference.jsx
```javascript
import React, { useState } from "react";
import {
import { Add as AddIcon, Edit as EditIcon } from "@mui/icons-material";
import DataTable from "../common/tables/DataTable";
import LoaderScreen from "../dashboard/LoaderScreen";
import StatusChip from "../common/StatusChip";
import { useBillingPreferences } from "../../hooks/useBilling";
import { customerService } from "../../services/customerService";
import { formatCustomDate } from "../../utils/dateUtils";
import BillingPreferenceSidebar from "./drawers/BillingPreferenceSidebar";
import AddPreferenceDialog from "./dialogs/AddPreferenceDialog";
import CustomSnackbar from "../layout/CustomSnackbar";
function BillingPreference() // [logic hidden]
```

### 📄 src/components/billing/dialogs/AddPreferenceDialog.jsx
```javascript
import React, { useState, useEffect } from "react";
import {
import { DatePicker } from "@mui/x-date-pickers/DatePicker";
import { format } from "date-fns";
import { useCreateBillingPreference, useUpdateBillingPreference } from "../../../hooks/useBilling";
import { customerService } from "../../../services/customerService";
import CustomSnackbar from "../../layout/CustomSnackbar";
function AddPreferenceDialog({ open, onClose, onSuccess, preference }) // [logic hidden]
```

### 📄 src/components/billing/dialogs/ProcessOrdersDialog.jsx
```javascript
import React, { useState, useEffect } from "react";
import {
import { DatePicker } from "@mui/x-date-pickers/DatePicker";
import { useProcessOrdersToBillable } from "../../../hooks/useBilling";
import { customerService } from "../../../services/customerService";
import CustomSnackbar from "../../layout/CustomSnackbar";
function ProcessOrdersDialog({ open, onClose, onSuccess }) // [logic hidden]
```

### 📄 src/components/billing/drawers/BillingCycleSidebar.jsx
```javascript
import React, { useState } from "react";
import {
import CloseIcon from "@mui/icons-material/Close";
import DeleteOutlineIcon from "@mui/icons-material/DeleteOutline";
import CustomDrawer from "../../common/CustomDrawer";
import StatusChip from "../../common/StatusChip";
import ConfirmDialog from "../../common/ConfirmDialog";
import CustomSnackbar from "../../layout/CustomSnackbar";
import { formatCustomDate, DATE_TIME } from "../../../utils/dateUtils";
import { billingService } from "../../../services/billingService";
import LockIcon from "@mui/icons-material/Lock";
import DescriptionIcon from "@mui/icons-material/Description";
import DownloadIcon from "@mui/icons-material/Download";
import { 
function BillingCycleSidebar({ open, cycle, loading: preliminaryLoading, onClose, onDeleted }) // [logic hidden]
```

### 📄 src/components/billing/drawers/BillingPreferenceSidebar.jsx
```javascript
import React from "react";
import {
import CloseIcon from "@mui/icons-material/Close";
import CustomDrawer from "../../common/CustomDrawer";
import StatusChip from "../../common/StatusChip";
import { formatCustomDate, DATE_TIME } from "../../../utils/dateUtils";
function BillingPreferenceSidebar({ open, preference, loading, onClose }) // [logic hidden]
```

### 📄 src/components/common/ConditionChip.jsx
```javascript
import StatusChip from './StatusChip';
function getConditionChipColor(condition) // [logic hidden]
export default function ConditionChip({ condition, ...props }) // [logic hidden]
```

### 📄 src/components/common/ConfirmDialog.jsx
```javascript
import {
export default function ConfirmDialog( // [logic hidden]
```

### 📄 src/components/common/CustomDrawer.jsx
```javascript
import { Drawer } from "@mui/material";
export default function CustomDrawer({ // [logic hidden]
```

### 📄 src/components/common/GreenButton.jsx
```javascript
import React from "react";
import { Button } from "@mui/material";
export default function GreenButton({ children = "Apply", sx, ...props }) // [logic hidden]
```

### 📄 src/components/common/ImageUploadPanel.jsx
```javascript
import React from "react";
import {
import UploadIcon from "@mui/icons-material/Upload";
import CloseIcon from "@mui/icons-material/Close";
export default function ImageUploadPanel( // [logic hidden]
```

### 📄 src/components/common/StatusChip.jsx
```javascript
import React from "react";
import { Chip } from "@mui/material";
import { getStatusChipColor } from "../../utils/statusUtils";
export default function StatusChip({ status, ...props }) // [logic hidden]
```

### 📄 src/components/common/TableCell.jsx
```javascript
import React, { useState } from "react";
import {
import SensorsIcon from "@mui/icons-material/Sensors";
import ScannerPicker from "../Scanner/ScannerPicker";
import ScannedIdsIndicator from "../Scanner/ScannedIdsIndicator";
```

### 📄 src/components/common/TabsHeader.jsx
```javascript
import { Box, Tabs, Tab } from "@mui/material";
import { useNavigate } from "react-router-dom";
export default function TabsHeader({ tabs = [], value }) // [logic hidden]
```

### 📄 src/components/common/tables/DataRow.jsx
```javascript
import { TableCell, TableRow, Tooltip } from "@mui/material";
import React from "react";
export const DataRow = React.memo(function DataRow( // [logic hidden]
function areEqualRow(prevProps, nextProps) // [logic hidden]
```

### 📄 src/components/common/tables/DataTable.jsx
```javascript
import {
import { DataRow } from "./DataRow";
import { useState } from "react";
import AddIcon from "@mui/icons-material/Add";
import RemoveIcon from "@mui/icons-material/Remove";
import theme from "../../../theme";
export default function DataTable( // [logic hidden]
  rowSx = () => ({}), // function to style row: (row) => sxObject // [logic hidden]
  cellSx = () => ({}), // function to style cell: (value, row, col) => sxObject // [logic hidden]
```

### 📄 src/components/customers/CustomerDetails.jsx
```javascript
import React, { useEffect, useState } from 'react';
import { Drawer, Typography, List, ListItem, ListItemText, Divider } from '@mui/material';
import { agreementService } from '../../services/agreementService'; // Assuming you have a service to fetch agreements
function CustomerDetails({ customer, onClose }) // [logic hidden]
```

### 📄 src/components/customers/CustomerFormDialog.jsx
```javascript
import React, { useState, useEffect } from 'react';
import { Dialog, DialogTitle, DialogContent, DialogActions, Button, TextField, Grid } from '@mui/material';
function CustomerFormDialog({ open, onClose, onSave, customer }) // [logic hidden]
```

### 📄 src/components/customers/CustomerList.jsx
```javascript
import React, { useState, useEffect } from 'react';
import {
import DeleteIcon from '@mui/icons-material/Delete';
import EditIcon from '@mui/icons-material/Edit';
import AddIcon from '@mui/icons-material/Add';
import BusinessIcon from '@mui/icons-material/Business';
import CheckCircleIcon from '@mui/icons-material/CheckCircle';
import CancelIcon from '@mui/icons-material/Cancel';
import SearchIcon from '@mui/icons-material/Search';
import { customerService } from '../../services/customerService'; // Assume you have a customerService
import CustomerDetails from './CustomerDetails'; // Import the sidebar component
function CustomerList() // [logic hidden]
```

### 📄 src/components/customers/CustomerTable.jsx
```javascript
import React from 'react';
import { Table, TableBody, TableCell, TableContainer, TableHead, TableRow, IconButton } from '@mui/material';
import { Edit as EditIcon, Delete as DeleteIcon } from '@mui/icons-material';
function CustomerTable({ customers, onEdit, onDelete }) // [logic hidden]
```

### 📄 src/components/customers/CustomerUserDialog.jsx
```javascript
import React, { useState, useEffect } from 'react';
import {
import CloseIcon from '@mui/icons-material/Close';
function CustomerUserDialog({ open, onClose, onSave, user = null, customerId }) // [logic hidden]
```

### 📄 src/components/customers/CustomerUsersTable.jsx
```javascript
import React, { useState, useEffect } from 'react';
import {
import AddIcon from '@mui/icons-material/Add';
import EditIcon from '@mui/icons-material/Edit';
import DeleteIcon from '@mui/icons-material/Delete';
import { customerUserService } from '../../services/customerUserService';
import CustomerUserDialog from './CustomerUserDialog';
function CustomerUsersTable({ customerId, customerName }) // [logic hidden]
```

### 📄 src/components/customers/CustomersPage.jsx
```javascript
import React, { useState, useEffect } from 'react';
import { customerService } from '../../services/customerService';
import CustomerTable from './CustomerTable';
import CustomerFormDialog from './CustomerFormDialog';
import { Button, Container, Paper } from '@mui/material';
function CustomersPage() // [logic hidden]
```

### 📄 src/components/damageAssessment/DamageAssessmentDashboard.jsx
```javascript
import React, { useState } from "react";
import {
import {
import { DatePicker } from "@mui/x-date-pickers/DatePicker";
import { format, subDays } from "date-fns";
import {
import { formatDateForApi } from "../../utils/dateUtils";
import { DAMAGE_STATUSES } from "../../constants/damageAssessment";
import ItemDamageRequestDialog from "./ItemDamageRequestDialog";
import DamageAssessmentSidebar from "./DamageAssessmentSidebar";
import DataTable from "../common/tables/DataTable";
import LoaderScreen from "../dashboard/LoaderScreen";
import StatusChip from "../common/StatusChip";
import GreenButton from "../common/GreenButton";
function DamageAssessmentDashboard() // [logic hidden]
    { field: "sourceName", headerName: "Source Name", type: "text", width: 150 },
    { field: "sourceType", headerName: "Source Type", type: "text", width: 130 },
    { field: "productName", headerName: "Product", type: "text", width: 150 },
    { field: "quantity", headerName: "Quantity", type: "smallNumber", width: 90 },
      type: "text",
      type: "shortText",
      type: "smallText",
```

### 📄 src/components/damageAssessment/DamageAssessmentSidebar.jsx
```javascript
import React from "react";
import {
import CloseIcon from "@mui/icons-material/Close";
import { DATE_TIME, formatCustomDate } from "../../utils/dateUtils";
import VisitImagesDialog from "../trips/VisitImagesDialog";
import CustomDrawer from "../common/CustomDrawer";
import StatusChip from "../common/StatusChip";
import { DAMAGE_STATUS } from "../../constants/damageAssessment";
import {
function DamageAssessmentSidebar({ open, damage, loading, onClose }) // [logic hidden]
```

### 📄 src/components/damageAssessment/ItemDamageRequestDialog.jsx
```javascript
import React, { useEffect, useState, useMemo } from "react";
import {
import { useForm } from "react-hook-form";
import { getDayRange } from "../../utils/dateUtils";
import {
import { useDcid } from "../../context/DcidContext";
import { DAMAGE_SOURCE, SOURCE_ENTITY_TYPE } from "../../constants/damageAssessment";
import DamageSourcePanel from "./create/DamageSourcePanel";
import DamageItemPanel from "./create/DamageItemPanel";
function ItemDamageRequestDialog({ open, onClose, initialData }) // [logic hidden]
        const { issueService } = await import("../../services/issueService");
```

### 📄 src/components/damageAssessment/create/DamageItemPanel.jsx
```javascript
import React from "react";
import {
import UploadIcon from "@mui/icons-material/Upload";
import CloseIcon from "@mui/icons-material/Close";
import { Controller } from "react-hook-form";
function DamageItemPanel( // [logic hidden]
```

### 📄 src/components/damageAssessment/create/DamageSourcePanel.jsx
```javascript
import React, { useMemo } from "react";
import {
import { DatePicker } from "@mui/x-date-pickers/DatePicker";
import { Controller } from "react-hook-form";
import { useSearchCustomers, useSearchLaundryVendors } from "../../../hooks/useDamageAssessment";
import { DAMAGE_SOURCE, DAMAGE_SOURCE_TYPES } from "../../../constants/damageAssessment";
function DamageSourcePanel( // [logic hidden]
                            {DAMAGE_SOURCE_TYPES.map((type) => (
                                <MenuItem key={type.value} value={type.value}>{type.label}</MenuItem>
                            ))}
```

### 📄 src/components/dashboard/ChartCard.jsx
```javascript
import React from "react";
import { Paper, Stack, Box, Typography, Divider } from "@mui/material";
import EmptyState from "./EmptyState";
import ChartSkeleton from "./ChartSkeleton";
export default function ChartCard({ title, subtitle, children, actions, isLoading, isEmpty }) // [logic hidden]
```

### 📄 src/components/dashboard/ChartSkeleton.jsx
```javascript
import React from "react";
import { Box, Skeleton } from "@mui/material";
export default function ChartSkeleton() // [logic hidden]
```

### 📄 src/components/dashboard/Dashboard.jsx
```javascript
import React, { useEffect, useMemo, useState, useCallback } from "react";
import { fetchDashboardData } from "../../utils/dashboardData";
import { Box, Paper, Typography, IconButton, Stack, Divider, Tooltip, Chip } from "@mui/material";
import { useTheme } from "@mui/material/styles";
import { BarChart } from "@mui/x-charts/BarChart";
import { LocalizationProvider } from "@mui/x-date-pickers/LocalizationProvider";
import { DatePicker } from "@mui/x-date-pickers/DatePicker";
import { AdapterDateFns } from "@mui/x-date-pickers/AdapterDateFns";
import RefreshIcon from "@mui/icons-material/Refresh";
import TodayIcon from "@mui/icons-material/Today";
import CalendarMonthIcon from "@mui/icons-material/CalendarMonth";
import FilterAltOutlinedIcon from "@mui/icons-material/FilterAltOutlined";
import { format, startOfDay, endOfDay, subDays } from "date-fns";
import Section from "./Section";
import MetricCard from "./MetricCard";
import ChartCard from "./ChartCard";
import LoaderScreen from "./LoaderScreen";
import ErrorState from "./ErrorState";
export default function Dashboard() // [logic hidden]
```

### 📄 src/components/dashboard/EmptyState.jsx
```javascript
import React from "react";
import { Stack, Box, Typography } from "@mui/material";
import CheckCircleOutlineIcon from "@mui/icons-material/CheckCircleOutline";
export default function EmptyState({ icon = <CheckCircleOutlineIcon />, title = "All clear", subtitle }) // [logic hidden]
```

### 📄 src/components/dashboard/ErrorState.jsx
```javascript
import React from "react";
import EmptyState from "./EmptyState";
import ErrorOutlineIcon from "@mui/icons-material/ErrorOutline";
export default function ErrorState({ error }) // [logic hidden]
```

### 📄 src/components/dashboard/LoaderScreen.jsx
```javascript
import React, { Fragment } from "react";
import { Box, GlobalStyles } from "@mui/material";
export default function LoaderScreen() // [logic hidden]
```

### 📄 src/components/dashboard/MetricCard.jsx
```javascript
import React from "react";
import { Paper, Typography, Box } from "@mui/material";
export default function MetricCard({ label, value, accent, caption }) // [logic hidden]
```

### 📄 src/components/dashboard/Section.jsx
```javascript
import React from "react";
import { Box, Stack, Typography } from "@mui/material";
export default function Section({ title, actions, children, sx }) // [logic hidden]
```

### 📄 src/components/hotels/HotelDialog.jsx
```javascript
import React, { useState, useEffect } from 'react';
import {
import AddIcon from '@mui/icons-material/Add';
import DeleteIcon from '@mui/icons-material/Delete';
import { customerService } from '../../services/customerService';
import { useAgentForm } from '../../agent/useAgentForm';
function HotelDialog({ open, onClose, onSave, hotel }) // [logic hidden]
    type: 'HOTEL',
        type: hotel.type || 'HOTEL',
        type: 'HOTEL',
  const handleAddressChange = (type, field, value) => {
    setHotelFormData(prev => ({
      ...prev,
      [type]: {
        ...prev[type],
        [field]: value
      }
    }));
  };
      type: 'HOTEL',
      { key: "name", type: "text", set: (v) => handleHotelFormChange("name", v) },
      { key: "phone", type: "text", set: (v) => handleHotelFormChange("phone", v) },
      { key: "email", type: "text", set: (v) => handleHotelFormChange("email", v) },
      { key: "status", type: "select", set: (v) => handleHotelFormChange("status", v) },
      { key: "type", type: "select", set: (v) => handleHotelFormChange("type", v) },
      { key: "gstin", type: "text", set: (v) => handleHotelFormChange("gstin", v) },
      { key: "pan", type: "text", set: (v) => handleHotelFormChange("pan", v) },
      { key: "billingAddressLine1", type: "text", set: (v) => handleAddressChange("billingAddress", "addressLine1", v) },
      { key: "billingAddressLine2", type: "text", set: (v) => handleAddressChange("billingAddress", "addressLine2", v) },
      { key: "billingCity", type: "text", set: (v) => handleAddressChange("billingAddress", "city", v) },
      { key: "billingState", type: "text", set: (v) => handleAddressChange("billingAddress", "state", v) },
      { key: "billingPincode", type: "text", set: (v) => handleAddressChange("billingAddress", "pincode", v) },
      { key: "billingCountry", type: "text", set: (v) => handleAddressChange("billingAddress", "country", v) },
      { key: "shippingAddressLine1", type: "text", set: (v) => handleAddressChange("shippingAddress", "addressLine1", v) },
      { key: "shippingAddressLine2", type: "text", set: (v) => handleAddressChange("shippingAddress", "addressLine2", v) },
      { key: "shippingCity", type: "text", set: (v) => handleAddressChange("shippingAddress", "city", v) },
      { key: "shippingState", type: "text", set: (v) => handleAddressChange("shippingAddress", "state", v) },
      { key: "shippingPincode", type: "text", set: (v) => handleAddressChange("shippingAddress", "pincode", v) },
      { key: "shippingCountry", type: "text", set: (v) => handleAddressChange("shippingAddress", "country", v) },
          { key: "contactName", type: "text", setByIndex: (v, idx) => handleContactPersonChange(idx, "name", v) },
          { key: "contactPhone", type: "text", setByIndex: (v, idx) => handleContactPersonChange(idx, "phone", v) },
          { key: "contactEmail", type: "text", setByIndex: (v, idx) => handleContactPersonChange(idx, "email", v) },
          { key: "contactTag", type: "text", setByIndex: (v, idx) => handleContactPersonChange(idx, "tag", v) },
                    {customerTypes.map((type) => (
                      <MenuItem key={type} value={type}>
                        {type}
                      </MenuItem>
                    ))}
```

### 📄 src/components/hotels/HotelList.jsx
```javascript
import React, { useState, useEffect, useCallback, useMemo, useRef } from "react";
import debounce from "lodash.debounce";
import {
import AddIcon from "@mui/icons-material/Add";
import DeleteIcon from "@mui/icons-material/Delete";
import EditIcon from "@mui/icons-material/Edit";
import SearchIcon from "@mui/icons-material/Search";
import { customerService } from "../../services/customerService";
import { agreementService } from "../../services/agreementService";
import { productService } from "../../services/productService";
import { useDcid } from "../../context/DcidContext";
import HotelDialog from "./HotelDialog"; // Import the dialog component
import CustomSnackbar from "../layout/CustomSnackbar"; // Import the CustomSnackbar component
import CustomerUsersTable from "../customers/CustomerUsersTable";
import { formatCustomDate } from "../../utils/dateUtils";
import { useAgentForm } from '../../agent/useAgentForm';
function HotelList() // [logic hidden]
        type: "",
                type: agreementResponse.type,
                type: "",
                type: agreementFormData.type,
            type: event.target.value,
            type: "",
            { key: 'type', type: 'select', set: v => handleAgreementFormChange('type', v) },
            { key: 'startDate', type: 'date', set: v => handleAgreementFormChange('startDate', v) },
            { key: 'endDate', type: 'date', set: v => handleAgreementFormChange('endDate', v) },
            { key: 'status', type: 'select', set: v => handleAgreementFormChange('status', v) },
            { key: 'totalRooms', type: 'text', set: v => handleAgreementFormChange('totalRooms', v) },
            { key: 'occupancyRate', type: 'text', set: v => handleAgreementFormChange('occupancyRate', v) },
            { key: 'depositAmount', type: 'text', set: v => handleAgreementFormChange('depositAmount', v) },
            { key: 'creditDays', type: 'text', set: v => handleAgreementFormChange('creditDays', v) },
            { key: 'linenDeliveryDays', type: 'text', set: v => handleAgreementFormChange('linenDeliveryDays', v) },
            { key: 'serviceFrequency', type: 'select', set: v => handleAgreementFormChange('serviceFrequency', v) },
            { key: 'creditTermDays', type: 'text', set: v => handleAgreementFormChange('creditTermDays', v) },
            { key: 'pickupFrequencyDays', type: 'text', set: v => handleAgreementFormChange('pickupFrequencyDays', v) },
            { key: 'deliveryTatDays', type: 'text', set: v => handleAgreementFormChange('deliveryTatDays', v) },
            { key: 'discountPercentage', type: 'text', set: v => handleAgreementFormChange('discountPercentage', v) },
            { key: 'billingCycle', type: 'text', set: v => handleAgreementFormChange('billingCycle', v) },
            { key: 'billingStartDay', type: 'text', set: v => handleAgreementFormChange('billingStartDay', v) },
            { key: 'billingEndDay', type: 'text', set: v => handleAgreementFormChange('billingEndDay', v) },
            { key: 'billingType', type: 'select', set: v => handleAgreementFormChange('billingType', v) },
            { key: 'fixedMonthlyAmount', type: 'text', set: v => handleAgreementFormChange('fixedMonthlyAmount', v) },
                    { key: 'productId', type: 'select', setByIndex: (val, idx) => {
                        let productId = val;
                        if (typeof val === 'string') {
                            const prod = products.find(p => p.name.toLowerCase() === val.toLowerCase() || p.id === val);
                            if (prod) productId = prod.id;
                        } else if (val && val.id) {
                            productId = val.id;
                        }
                        handlePriceItemChange(idx, 'productId', productId);
                    }},
                    { key: 'quantity', type: 'text', setByIndex: (v, i) => handlePriceItemChange(i, 'quantity', v) },
                    { key: 'price', type: 'text', setByIndex: (v, i) => handlePriceItemChange(i, 'price', v) },
                    { key: 'serviceType', type: 'select', setByIndex: (v, i) => handlePriceItemChange(i, 'serviceType', v) },
                    { key: 'remarks', type: 'text', setByIndex: (v, i) => handlePriceItemChange(i, 'remarks', v) },
                                type: "",
                                                    {serviceTypes.map((type) => (
                                                        <MenuItem key={type} value={type}>
                                                            {type.replace("_", " ")}
                                                        </MenuItem>
                                                    ))}
```

### 📄 src/components/hotels/HotelsPage.jsx
```javascript
import React, { useState, useEffect } from 'react';
import { customerService } from '../../services/customerService';
import { Table, TableBody, TableCell, TableContainer, TableHead, TableRow, Paper, IconButton } from '@mui/material';
import { Edit as EditIcon, Delete as DeleteIcon } from '@mui/icons-material';
import CustomerFormDialog from '../customers/CustomerFormDialog';
import { Button, Container } from '@mui/material';
function HotelsPage() // [logic hidden]
```

### 📄 src/components/inventory/BulkSearchDialog.jsx
```javascript
import { useState, useEffect, useMemo } from 'react';
import {
import {
import { parseItemIds } from '../../utils/inventoryUtils';
export default function BulkSearchDialog({ open, onClose, onSearch, initialValue = '' }) // [logic hidden]
```

### 📄 src/components/inventory/InventoryDetails.jsx
```javascript
import { useEffect, useMemo, useState } from "react";
import {
import { Close as CloseIcon } from "@mui/icons-material";
import { DATE_TIME, formatCustomDate } from "../../utils/dateUtils";
import StatusChip from "../common/StatusChip";
import DataTable from "../common/tables/DataTable";
import { inventoryService } from "../../services/inventoryService";
function InventoryDetails({ item, onClose }) // [logic hidden]
    { field: "inventoryItemId", headerName: "Inventory Item ID", type: "number" },
    { field: "fromLocationType", headerName: "From Location Type", type: "shortText" },
      type: "shortText",
    { field: "toLocationType", headerName: "To Location Type", type: "shortText" },
      type: "shortText",
      type: "shortText",
      type: "shortText",
    { field: "remarks", headerName: "Remarks", type: "text", isPrimary: false },
```

### 📄 src/components/inventory/InventoryList.jsx
```javascript
import { formatCustomDate } from '../../utils/dateUtils';
import { getActivePoolItems } from '../../utils/inventoryUtils';
import DataTable from '../common/tables/DataTable';
import StatusChip from '../common/StatusChip';
import ConditionChip from '../common/ConditionChip';
import WarningIcon from '@mui/icons-material/Warning';
function InventoryList( // [logic hidden]
    { field: 'id', headerName: 'Item ID', type: 'smallNumber' },
      type: 'smallNumber',
    { field: 'productName', headerName: 'Product Name', type: 'mediumText' },
    { field: 'warehouseName', headerName: 'Warehouse', type: 'mediumText' },
      type: 'smallText',
      type: 'smallText',
      type: 'smallText',
      type: 'mediumText',
```

### 📄 src/components/inventory/InventoryPage.jsx
```javascript
import { useEffect, useState, useMemo, useCallback } from 'react';
import { useSearchParams } from 'react-router-dom';
import {
import { Search as SearchIcon, PlaylistAdd as BulkIcon } from '@mui/icons-material';
import { productService } from '../../services/productService';
import { inventoryService } from '../../services/inventoryService';
import { parseItemIds } from '../../utils/inventoryUtils';
import CopyInventoryButton from '../../utils/CopyInventoryButton';
import InventoryList from './InventoryList';
import InventoryDetails from './InventoryDetails';
import InventoryPoolItemDetails from './InventoryPoolItemDetails';
import LoaderScreen from '../dashboard/LoaderScreen';
import TabsHeader from '../common/TabsHeader';
import GreenButton from '../common/GreenButton';
import CustomDrawer from '../common/CustomDrawer';
import BulkSearchDialog from './BulkSearchDialog';
function InventoryPage() // [logic hidden]
    async function fetchData() // [logic hidden]
          onSelect={(item) => setSelectedDetails({ type: 'ITEM', item })}
          onSelectPoolItem={(item) => setSelectedDetails({ type: 'POOL_ITEM', item })}
```

### 📄 src/components/inventory/InventoryPoolItemDetails.jsx
```javascript
import { useEffect, useMemo, useState } from "react";
import {
import { Close as CloseIcon } from "@mui/icons-material";
import { DATE_TIME, formatCustomDate } from "../../utils/dateUtils";
import StatusChip from "../common/StatusChip";
import DataTable from "../common/tables/DataTable";
import { inventoryService } from "../../services/inventoryService";
function InventoryPoolItemDetails({ item, selectedPoolItemId, onClose }) // [logic hidden]
    { field: "fromStatus", headerName: "From Status", type: "shortText" },
    { field: "toStatus", headerName: "To Status", type: "shortText" },
    { field: "fromCondition", headerName: "From Condition", type: "shortText" },
    { field: "toCondition", headerName: "To Condition", type: "shortText" },
      type: "shortText",
    { field: "reason", headerName: "Reason", type: "text", isPrimary: false },
```

### 📄 src/components/inventory-pool/AddTransactionDialog.jsx
```javascript
import {
import { DateTimePicker } from "@mui/x-date-pickers/DateTimePicker";
import { useEffect, useState } from "react";
import { inventoryService } from "../../services/inventoryService";
import QuantityScanInput from "../Scanner/QuantityScanInput";
export default function AddTransactionDialog({ product, onSave, open, onClose }) // [logic hidden]
            {transactionTypes.map((type) => (
              <MenuItem key={type} value={type}>
                {type}
              </MenuItem>
            ))}
            {QUANTITY_TYPES.map((type) => (
              <MenuItem key={type} value={type}>
                {type}
              </MenuItem>
            ))}
```

### 📄 src/components/inventory-pool/CreatePoolDialog.jsx
```javascript
import { useEffect, useState } from 'react';
import {
import { inventoryService } from '../../services/inventoryService';
import productService from '../../services/productService';
export default function CreatePoolDialog({ open, onClose, onSave }) // [logic hidden]
```

### 📄 src/components/inventory-pool/CustomerInventoryDrawer.jsx
```javascript
import { Box, Typography, Divider, Chip } from "@mui/material";
import { useState, useEffect } from "react";
import DataTable from "../common/tables/DataTable";
import CustomDrawer from "../common/CustomDrawer";
import { inventoryService } from "../../services/inventoryService";
import { createColumn } from "../../utils/createColumn";
export default function CustomerInventoryDrawer({ product, isOpen, onClose }) // [logic hidden]
```

### 📄 src/components/inventory-pool/InventoryPoolPage.jsx
```javascript
import TabsHeader from "../common/TabsHeader";
import { Container } from "@mui/material";
import { useLocation } from "react-router-dom";
import { Outlet } from "react-router-dom";
function InventoryPoolPage() // [logic hidden]
```

### 📄 src/components/inventory-pool/InventoryPoolTable.jsx
```javascript
import {
import { useEffect, useState } from "react";
import DataTable from "../common/tables/DataTable";
import { inventoryService } from "../../services/inventoryService";
import CustomerInventoryDrawer from "./CustomerInventoryDrawer";
import InventoryPoolTransactionDrawer from "./InventoryPoolTransactionDrawer";
import { transformPoolsWithProducts } from "../../utils/poolTransformers";
import AddIcon from "@mui/icons-material/Add";
import CreatePoolDialog from "./CreatePoolDialog";
function InventoryPoolTable() // [logic hidden]
    { field: "id", headerName: "ID", type: "id" },
      type: "shortText",
      type: "shortText",
      type: "smallNumber",
      type: "smallNumber",
    { field: "soiledQuantity", headerName: "Qty Soiled", type: "smallNumber" },
    { field: "heavySoiledQuantity", headerName: "Qty H. Soiled", type: "smallNumber" },
    { field: "damagedQuantity", headerName: "Damaged", type: "smallNumber" },
      type: "smallNumber",
      type: "smallNumber",
```

### 📄 src/components/inventory-pool/InventoryPoolTransactionDrawer.jsx
```javascript
import { Box, Button, Divider, Typography } from "@mui/material";
import DataTable from "../common/tables/DataTable";
import AddTransactionDialog from "./AddTransactionDialog";
import InwardInventoryItemsToPoolDialog from "./InwardInventoryItemsToPoolDialog";
import { useState, useEffect } from "react";
import { inventoryService } from "../../services/inventoryService";
import { formatCustomDate, DATE_TIME } from "../../utils/dateUtils";
import CustomDrawer from "../common/CustomDrawer";
import AddIcon from "@mui/icons-material/Add";
    type: "number",
  { field: "transactionQuantity", headerName: "Qty", type: "smallNumber" },
    type: "number",
    type: "shortText",
export default function InventoryPoolTransactionDrawer( // [logic hidden]
```

### 📄 src/components/inventory-pool/InwardInventoryItemsToPoolDialog.jsx
```javascript
import {
import AddIcon from "@mui/icons-material/Add";
import RemoveCircleOutlineIcon from "@mui/icons-material/RemoveCircleOutline";
import { useEffect, useMemo, useState } from "react";
import { inventoryService } from "../../services/inventoryService";
function TabPanel({ value, index, children }) // [logic hidden]
export default function InwardInventoryItemsToPoolDialog( // [logic hidden]
```

### 📄 src/components/inventoryItemReservation/InventoryItemReservtionPage.jsx
```javascript
import React, { useEffect, useState, useMemo } from 'react';
import debounce from 'lodash.debounce';
import {
import { useLocation } from 'react-router-dom';
import { inventoryService } from '../../services/inventoryService';
import { reservationService } from '../../services/reservationService';
import { DATE_TIME, formatCustomDate } from '../../utils/dateUtils';
function InventoryItemReservtionPage() // [logic hidden]
```

### 📄 src/components/inventorySummary/CreateInwardRequestInventoryDialog.jsx
```javascript
import React, { useState, useEffect } from 'react';
import {
import AddIcon from '@mui/icons-material/Add';
import DeleteIcon from '@mui/icons-material/Delete';
import { inventoryService } from '../../services/inventoryService';
import { productService } from '../../services/productService';
import { vendorService } from '../../services/vendorService';
import { createInwardRequest } from '../../services/inwardRequestService';
function CreateInwardRequestInventoryDialog({ open, onClose, onSave, warehouses }) // [logic hidden]
```

### 📄 src/components/inventorySummary/InventorySummaryPage.jsx
```javascript
import React, { useEffect, useState } from "react";
import { Box, Container } from "@mui/material";
import { useNavigate } from "react-router-dom";
import { inventorySummaryService } from "../../services/inventorySummaryService";
import { inventoryService } from "../../services/inventoryService";
import CreateInwardRequestInventoryDialog from "./CreateInwardRequestInventoryDialog";
import InventoryTabs from "./InventoryTabs";
import InventorySummaryTable from "./InventorySummaryTable";
import InwardRequestsTable from "./InwardRequestsTable";
import InwardRequestDrawer from "./InwardRequestDrawer";
function InventorySummaryPage() // [logic hidden]
```

### 📄 src/components/inventorySummary/InventorySummaryTable.jsx
```javascript
import React from "react";
import {
function InventorySummaryTable({ inventorySummary }) // [logic hidden]
```

### 📄 src/components/inventorySummary/InventoryTabs.jsx
```javascript
import React from "react";
import { Tabs, Tab, Button, Box } from "@mui/material";
import AddIcon from "@mui/icons-material/Add";
import VisibilityIcon from "@mui/icons-material/Visibility";
function InventoryTabs({ tabIndex, setTabIndex, navigate, setCreateDialogOpen }) // [logic hidden]
```

### 📄 src/components/inventorySummary/InwardRequestDrawer.jsx
```javascript
import React from "react";
import {
function InwardRequestDrawer({ inwardRequest, onClose }) // [logic hidden]
```

### 📄 src/components/inventorySummary/InwardRequestsTable.jsx
```javascript
import React from "react";
import {
function InwardRequestsTable({ inwardRequests, onSelect }) // [logic hidden]
```

### 📄 src/components/invoices/AddPaymentDialog.jsx
```javascript
import React, { useState } from 'react';
import {
import { DatePicker } from '@mui/x-date-pickers/DatePicker';
function AddPaymentDialog({ open, onClose, onSave, remainingAmount }) // [logic hidden]
```

### 📄 src/components/invoices/CreateInvoiceDialog.jsx
```javascript
import React, { useState } from 'react';
import {
import { DatePicker } from '@mui/x-date-pickers/DatePicker';
import { formatCustomDate } from '../../utils/dateUtils';
function CreateInvoiceDialog({ open, onClose, onSave }) // [logic hidden]
```

### 📄 src/components/invoices/InvoiceDetails.jsx
```javascript
import { useState } from 'react';
import {
import {
import { keyframes } from '@mui/system';
import CustomDrawer from "../common/CustomDrawer";
import ConfirmDialog from "../common/ConfirmDialog";
import StatusChip from "../common/StatusChip";
import { formatCustomDate } from '../../utils/dateUtils';
import { invoiceService } from '../../services/invoiceService';
function InvoiceDetails({ invoice, onClose, onAddPayment, onInvoiceUpdated }) // [logic hidden]
```

### 📄 src/components/invoices/InvoiceFormDialog.jsx
```javascript
import React, { useState, useEffect } from 'react';
import { Dialog, DialogTitle, DialogContent, DialogActions, Button, TextField, Grid } from '@mui/material';
function InvoiceFormDialog({ open, onClose, onSave, invoice }) // [logic hidden]
```

### 📄 src/components/invoices/InvoiceGenerator.jsx
```javascript
import React, { useState, useEffect, useMemo } from "react";
import {
import { DatePicker } from "@mui/x-date-pickers/DatePicker";
import { orderService } from "../../services/orderService";
import { customerService } from "../../services/customerService";
import { invoiceService } from "../../services/invoiceService";
import { debounce } from "lodash";
import CustomSnackbar from "../layout/CustomSnackbar";
import { format, isValid as isValidDate } from "date-fns";
import { formatCustomDate } from "../../utils/dateUtils";
function InvoiceGenerator() // [logic hidden]
```

### 📄 src/components/invoices/InvoicesPage.jsx
```javascript
import React, { useState, useEffect, useCallback } from "react";
import {
import { DatePicker } from "@mui/x-date-pickers/DatePicker";
import {
import { keyframes } from "@mui/system";
import DataTable from "../common/tables/DataTable";
import StatusChip from "../common/StatusChip";
import GreenButton from "../common/GreenButton";
import InvoiceDetails from "./InvoiceDetails";
import AddPaymentDialog from "./AddPaymentDialog";
import InvoiceFormDialog from "./InvoiceFormDialog";
import ConfirmDialog from "../common/ConfirmDialog";
import CustomSnackbar from "../layout/CustomSnackbar";
import LoaderScreen from "../dashboard/LoaderScreen";
import { invoiceService } from "../../services/invoiceService";
import { customerService } from "../../services/customerService";
import { formatCustomDate, generateMonthOptions, getMonthRange } from "../../utils/dateUtils";
import { useGenerateInvoices } from "../../hooks/useBilling";
function InvoicesPage() // [logic hidden]
```

### 📄 src/components/issueTracker/CreateIssueDialog.jsx
```javascript
import React, { useEffect, useMemo, useRef, useState } from 'react';
import { format } from 'date-fns';
import { Box, Button, Dialog, DialogActions, DialogContent, DialogTitle } from '@mui/material';
import issueService from '../../services/issueService.jsx';
import productService from '../../services/productService.jsx';
import { customerService } from '../../services/customerService.jsx';
import { laundryVendorService } from '../../services/laundryVendorService.jsx';
import IssueDetailsPanel from './createIssue/IssueDetailsPanel.jsx';
import ItemAndImagesPanel from './createIssue/ItemAndImagesPanel.jsx';
import { useAgentForm } from '../../agent/useAgentForm';
export default function CreateIssueDialog({ open, onClose, onSubmit }) // [logic hidden]
        recordedDateTime: (function() { const d = asDate(form.recordedDateTime); return d ? format(d, "yyyy-MM-dd'T'00:00:00") : undefined; })(), // [logic hidden]
        ...(function buildItem() // [logic hidden]
    async function loadSources() // [logic hidden]
        type: "date",
        type: "select",
        type: "autocomplete",
        type: "select",
        type: "date",
        type: "select",
        type: "date",
        type: "select",
        type: "select",
        type: "text",
        type: "autocomplete",
        type: "number",
```

### 📄 src/components/issueTracker/IssueDetails.jsx
```javascript
import React from 'react';
import {
import { ReportProblem as IssueIcon, Close as CloseIcon } from '@mui/icons-material';
import VisitImagesDialog from '../trips/VisitImagesDialog.jsx';
import ResolveIssueDialog from './ResolveIssueDialog.jsx';
import { DATE_TIME, formatCustomDate } from '../../utils/dateUtils.js';
export default function IssueDetails({ issue, onClose, onResolved }) // [logic hidden]
```

### 📄 src/components/issueTracker/IssueFilters.jsx
```javascript
import React from 'react';
import { Box, Button, FormControl, InputLabel, MenuItem, Select } from '@mui/material';
import { DatePicker } from '@mui/x-date-pickers/DatePicker';
import SearchIcon from '@mui/icons-material/Search';
export default function IssueFilters({ filters, setFilters, onSearch, loading }) // [logic hidden]
```

### 📄 src/components/issueTracker/IssueTracker.jsx
```javascript
import React, { useEffect, useState } from 'react';
import { Box, Button, Container, Divider, Drawer, Paper, Typography } from '@mui/material';
import AddIcon from '@mui/icons-material/Add';
import RefreshIcon from '@mui/icons-material/Refresh';
import { format } from 'date-fns';
import Section from '../dashboard/Section';
import EmptyState from '../dashboard/EmptyState';
import ErrorState from '../dashboard/ErrorState';
import issueService from '../../services/issueService.jsx';
import IssueFilters from './IssueFilters';
import IssuesTable from './IssuesTable';
import CreateIssueDialog from './CreateIssueDialog';
import IssueDetails from './IssueDetails';
export default function IssueTracker() // [logic hidden]
```

### 📄 src/components/issueTracker/IssuesTable.jsx
```javascript
import {
import DeleteIcon from '@mui/icons-material/Delete';
import { formatCustomDate } from '../../utils/dateUtils';
export default function IssuesTable({ issues, onDelete, onRowClick }) // [logic hidden]
```

### 📄 src/components/issueTracker/ResolveIssueDialog.jsx
```javascript
import React from 'react';
import {
import { DateTimePicker } from '@mui/x-date-pickers/DateTimePicker';
import { format } from 'date-fns';
import issueService from '../../services/issueService.jsx';
export default function ResolveIssueDialog({ open, issueId, onClose, onResolved }) // [logic hidden]
        resolvedAt: (function() { const d = asDate(resolvedAt); return d ? format(d, "yyyy-MM-dd'T'HH:mm:ss") : undefined; })(), // [logic hidden]
```

### 📄 src/components/issueTracker/createIssue/IssueDetailsPanel.jsx
```javascript
import React, { useEffect, useMemo, useState } from 'react';
import { Box, MenuItem, Stack, TextField, Typography } from '@mui/material';
import { DatePicker } from '@mui/x-date-pickers/DatePicker';
import Autocomplete from '@mui/material/Autocomplete';
import { orderService } from '../../../services/orderService.jsx';
import { washFulfillmentService } from '../../../services/washFulfillmentService.jsx';
import { format } from 'date-fns';
export default function IssueDetailsPanel( // [logic hidden]
```

### 📄 src/components/issueTracker/createIssue/ItemAndImagesPanel.jsx
```javascript
import React from 'react';
import { Box, Button, CircularProgress, IconButton, Stack, TextField, Typography } from '@mui/material';
import UploadIcon from '@mui/icons-material/Upload';
import CloseIcon from '@mui/icons-material/Close';
import Autocomplete from '@mui/material/Autocomplete';
export default function ItemAndImagesPanel( // [logic hidden]
```

### 📄 src/components/layout/CustomSnackbar.jsx
```javascript
import React from 'react';
import {
import UndoIcon from '@mui/icons-material/Undo';
import CloseIcon from '@mui/icons-material/Close';
import CheckCircleIcon from '@mui/icons-material/CheckCircle';
import ErrorIcon from '@mui/icons-material/Error';
import InfoIcon from '@mui/icons-material/Info';
import WarningIcon from '@mui/icons-material/Warning';
function SlideTransition(props) // [logic hidden]
function CustomSnackbar( // [logic hidden]
```

### 📄 src/components/layout/Layout.jsx
```javascript
import React from "react";
import { Box, Drawer, Toolbar } from "@mui/material";
import { Outlet, useNavigate, useLocation } from "react-router-dom";
import Sidebar from "./Sidebar";
import { useDcid } from "../../context/DcidContext";
import LockOverlayUI from "./LockOverlayUI";
function Layout() // [logic hidden]
```

### 📄 src/components/layout/LockOverlayUI.jsx
```javascript
import { Box, Popper, Paper, Typography } from "@mui/material";
import { useDcid } from "../../context/DcidContext";
import { useEffect, useState } from "react";
export default function LockOverlayUI() // [logic hidden]
```

### 📄 src/components/layout/Sidebar.jsx
```javascript
import React from "react";
import { Box, List, ListItem, ListItemButton, ListItemIcon, ListItemText, Divider, Typography, Avatar, Menu, MenuItem } from "@mui/material";
import {
import { matchPath } from "react-router-dom";
import ErrorIcon from '@mui/icons-material/Error';
import RuleIcon from '@mui/icons-material/Rule';
import AttachMoneyIcon from "@mui/icons-material/AttachMoney";
import WarehousePill from "./WarehousePill.jsx";
```

### 📄 src/components/layout/TablePrimitives.jsx
```javascript
import React from "react";
import { TableContainer, Paper, Table, TableCell, TableRow } from "@mui/material";
```

### 📄 src/components/layout/WarehousePill.jsx
```javascript
import React, { useEffect, useMemo, useState, useRef } from "react";
import {
import WarehouseIcon from "@mui/icons-material/StoreMallDirectory";
import CheckIcon from "@mui/icons-material/Check";
import KeyboardArrowDownIcon from "@mui/icons-material/KeyboardArrowDown";
import { useDcid } from "../../context/DcidContext.jsx";
import apiService from "../../services/apiService.jsx";
function getId(w) // [logic hidden]
function getCode(w) // [logic hidden]
function getDisplayName(w) // [logic hidden]
export default function WarehousePill( // [logic hidden]
```

### 📄 src/components/orders/CreateOrderDialog.jsx
```javascript
import React, { useState, useEffect, useRef, useCallback } from "react";
import { useAgentForm } from "../../agent/useAgentForm";
import {
import DeleteIcon from "@mui/icons-material/Delete";
import { debounce } from "lodash";
import { customerService } from "../../services/customerService";
import { orderService } from "../../services/orderService";
import { productService } from "../../services/productService";
import {
import CustomSnackbar from "../layout/CustomSnackbar";
function safeDateToISO(d) // [logic hidden]
function safeDateToFullISO(d) // [logic hidden]
function CreateOrderDialog({ open, onClose, onSave, order }) // [logic hidden]
  const setQtyRef = (type, index, el) => {
    if (!qtyRefs.current[type]) qtyRefs.current[type] = [];
    qtyRefs.current[type][index] = el;
  };
  const focusNextQty = useCallback((type, index) => {
    const list = qtyRefs.current[type] || [];
    const next = list[index + 1];
    if (next && typeof next.focus === "function") {
      next.focus();
      const val = next.value || "";
      next.setSelectionRange(val.length, val.length);
    }
  }, []);
        const type = formData.deliveryType.toUpperCase();
          type === "PICKUP"
          type === "DELIVERY"
          leasingOrderType: type,
  const renderItemRow = (item, index, type, isDisabled = false) => (
              handleItemChange(index, "productId", e.target.value, type)
            handleItemChange(index, "quantity", next, type);
              focusNextQty(type, index);
          inputRef={(el) => setQtyRef(type, index, el)}
            handleItemChange(index, "remarks", e.target.value, type)
          onClick={() => handleDeleteItem(index, type)}
        type: "text",
        type: "autocomplete",
        type: "date",
        type: "select",
        type: "toggle",
        type: "select",
        type: "date",
        type: "date",
            type: "select",
            type: "text",
            type: "text",
            type: "select",
            type: "text",
            type: "text",
```

### 📄 src/components/orders/DownloadWorkReportButton.jsx
```javascript
import React, { useState } from 'react';
import { Button, CircularProgress } from '@mui/material';
import DownloadIcon from '@mui/icons-material/Download';
import { format, startOfDay, endOfDay } from 'date-fns';
import { orderService } from '../../services/orderService';
import { downloadWorkReportXLSX } from '../../utils/workReport';
export default function DownloadWorkReportButton( // [logic hidden]
```

### 📄 src/components/orders/OrderDetailSidebar.jsx
```javascript
import React from "react";
import {
import TableCell from "../common/TableCell";
import {
import { format } from "date-fns";
import ReserveItemsDialog from "./ReserveItemsDialog";
import RejectItemsDialog from "./RejectItemsDialog";
import { orderService } from "../../services/orderService";
import VisitImagesDialog from "../trips/VisitImagesDialog";
import CustomSnackbar from "../layout/CustomSnackbar";
import { formatCustomDate } from "../../utils/dateUtils";
import CustomDrawer from "../common/CustomDrawer";
function OrderDetailSidebar({ order, onClose, onUpdateOrder }) // [logic hidden]
```

### 📄 src/components/orders/OrderList.jsx
```javascript
import React, { useState, useEffect } from "react";
import {
import { Add as AddIcon } from "@mui/icons-material";
import { DatePicker } from "@mui/x-date-pickers/DatePicker";
import { format, subDays, startOfDay, endOfDay } from "date-fns";
import debounce from "lodash.debounce";
import CreateOrderDialog from "./CreateOrderDialog";
import OrderDetailSidebar from "./OrderDetailSidebar";
import { orderService } from "../../services/orderService";
import { customerService } from "../../services/customerService";
import CustomSnackbar from "../layout/CustomSnackbar";
import LoaderScreen from "../dashboard/LoaderScreen";
import DownloadWorkReportButton from "./DownloadWorkReportButton";
import OrderRow from "./OrderRow";
function OrderList() // [logic hidden]
```

### 📄 src/components/orders/OrderRow.jsx
```javascript
import React, { useMemo } from "react";
import { DATE_TIME, formatCustomDate } from "../../utils/dateUtils";
import { Chip, IconButton, TableCell, TableRow } from "@mui/material";
import {
```

### 📄 src/components/orders/RejectItemsDialog.jsx
```javascript
import React, { useState, useEffect } from "react";
import {
import { orderService } from "../../services/orderService";
import UploadImages from "../orders/UploadImages";
import QuantityScanInput from "../Scanner/QuantityScanInput";
import ScannerHeader from "../Scanner/ScannerHeader";
function RejectItemsDialog( // [logic hidden]
```

### 📄 src/components/orders/ReserveItemsDialog.jsx
```javascript
import React, { useEffect, useState } from "react";
import {
import TableCell from "../common/TableCell";
import { inventoryService } from "../../services/inventoryService";
import CustomSnackbar from "../layout/CustomSnackbar";
import ScannerHeader from "../Scanner/ScannerHeader";
import ColumnScanHeaderAction from "../Scanner/ColumnScanHeaderAction";
import { scannerController } from "../Scanner/ScannerController";
import { normalizeQuantity } from "../../utils/quantityUtils";
```

### 📄 src/components/orders/UploadImages.jsx
```javascript
import React, { useState } from "react";
import { Box, Button, Paper, CircularProgress } from "@mui/material";
import ReplayIcon from "@mui/icons-material/Replay";
import CustomSnackbar from "../layout/CustomSnackbar";
import { issueService } from "../../services/issueService";
```

### 📄 src/components/processing/LaundryProcessingList.jsx
```javascript
import React, { useState } from 'react';
import {
import {
import { DatePicker } from '@mui/x-date-pickers/DatePicker';
import { format } from 'date-fns';
import ProcessingRequestDialog from './ProcessingRequestDialog';
function ProcessingRequestRow({ request }) // [logic hidden]
function SoiledInventoryCard({ products }) // [logic hidden]
function LaundryProcessingList() // [logic hidden]
```

### 📄 src/components/processing/ProcessingDetails.jsx
```javascript
import React from 'react';
import {
import {
import { format } from 'date-fns';
function ProcessingDetails({ process, onClose }) // [logic hidden]
```

### 📄 src/components/processing/ProcessingDialog.jsx
```javascript
import React, { useState, useEffect } from 'react';
import {
import {
import { DatePicker } from '@mui/x-date-pickers/DatePicker';
function ProcessingDialog({ open, onClose, onSave, process }) // [logic hidden]
```

### 📄 src/components/processing/ProcessingList.jsx
```javascript
import React, { useState, useEffect } from 'react';
import {
import {
import { DatePicker } from '@mui/x-date-pickers';
import { format } from 'date-fns';
import ProcessingRequestDialog from './ProcessingRequestDialog';
import ProcessingRequestDetails from './ProcessingRequestDetails';
import { soiledService } from '../../services/soiledService';
function SoiledInventoryCard({ items }) // [logic hidden]
function ProcessingList() // [logic hidden]
```

### 📄 src/components/processing/ProcessingPage.jsx
```javascript
import React, { useState, useEffect } from 'react';
import {
import { Add as AddIcon, Check as CheckIcon } from '@mui/icons-material';
import { processingService } from '../../services/processingService';
function ProcessingPage() // [logic hidden]
```

### 📄 src/components/processing/ProcessingRequestDetails.jsx
```javascript
import React from 'react';
import {
import CloseIcon from '@mui/icons-material/Close';
function ProcessingRequestDetails({ request, onClose }) // [logic hidden]
```

### 📄 src/components/processing/ProcessingRequestDialog.jsx
```javascript
import React, { useState, useEffect } from 'react';
import {
import { Delete as DeleteIcon } from '@mui/icons-material';
import { soiledService } from '../../services/soiledService';
import { productService } from '../../services/productService';
import { vendorService } from '../../services/vendorService';
function ProcessingRequestDialog({ open, onClose, onSave }) // [logic hidden]
```

### 📄 src/components/products/ProductDialog.jsx
```javascript
import React, { useState, useEffect } from 'react';
import {
import { LocalLaundryService as ProductIcon } from '@mui/icons-material';
import { productService } from '../../services/productService';
function ProductDialog({ open, onClose, onSave, product }) // [logic hidden]
```

### 📄 src/components/products/ProductList.jsx
```javascript
import React, { useState, useEffect } from 'react';
import {
import {
import { useTheme } from '@mui/material/styles';
import ProductDialog from './ProductDialog';
import { productService } from '../../services/productService';
import CustomSnackbar from '../layout/CustomSnackbar';
function ProductList() // [logic hidden]
```

### 📄 src/components/stocks/CreateInwardRequestDialog.jsx
```javascript
import React, { useState, useEffect } from 'react';
import {
import AddIcon from '@mui/icons-material/Add';
import DeleteIcon from '@mui/icons-material/Delete';
import { inventoryService } from '../../services/inventoryService';
import { productService } from '../../services/productService';
import { vendorService } from '../../services/vendorService';
import CustomSnackbar from '../layout/CustomSnackbar';
function CreateInwardRequestDialog({ open, onClose, onSave, buckets }) // [logic hidden]
```

### 📄 src/components/stocks/CreateReservationDialog.jsx
```javascript
import React, { useState, useEffect } from 'react';
import {
import AddIcon from '@mui/icons-material/Add';
import DeleteIcon from '@mui/icons-material/Delete';
import { inventoryService } from '../../services/inventoryService';
import { customerService } from '../../services/customerService';
import { productService } from '../../services/productService';
function CreateReservationDialog({ open, onClose, onSave }) // [logic hidden]
```

### 📄 src/components/stocks/ReservationDetails.jsx
```javascript
import {
function ReservationDetails({ reservation, onClose }) // [logic hidden]
```

### 📄 src/components/stocks/StockDetails.jsx
```javascript
import React, { useState, useEffect } from 'react';
import {
import CloseIcon from '@mui/icons-material/Close';
import { inventoryService } from '../../services/inventoryService';
function StockDetails({ stock, onClose }) // [logic hidden]
```

### 📄 src/components/stocks/StockInwardDialog.jsx
```javascript
import React, { useState, useEffect } from 'react';
import {
import { DateTimePicker } from '@mui/x-date-pickers/DateTimePicker';
import { productService } from '../../services/productService';
import { vendorService } from '../../services/vendorService';
function StockInwardDialog({ open, onClose, onSave }) // [logic hidden]
```

### 📄 src/components/stocks/StockList.jsx
```javascript
import React, { useState, useEffect } from 'react';
import {
import AddIcon from '@mui/icons-material/Add';
import { inventoryService } from '../../services/inventoryService';
import CreateInwardRequestDialog from './CreateInwardRequestDialog';
import StockDetails from './StockDetails';
import CreateReservationDialog from './CreateReservationDialog';
import { productService } from '../../services/productService';
import { vendorService } from '../../services/vendorService';
import CustomSnackbar from '../layout/CustomSnackbar';
function StockList() // [logic hidden]
```

### 📄 src/components/trips/CompleteDeliveryDialog.jsx
```javascript
import React, { useState, useEffect } from "react";
import {
import { DateTimePicker } from "@mui/x-date-pickers/DateTimePicker";
import {
import ScannerHeader from "../Scanner/ScannerHeader";
import ColumnScanHeaderAction from "../Scanner/ColumnScanHeaderAction";
import { scannerController } from "../Scanner/ScannerController";
import TableCell from "../common/TableCell";
import { parseDate } from "../../utils/dateUtils";
import tripService from "../../services/tripService";
function CompleteDeliveryDialog( // [logic hidden]
```

### 📄 src/components/trips/CompletePickupDialog.jsx
```javascript
import React, { useState, useEffect } from "react";
import {
import { DateTimePicker } from "@mui/x-date-pickers/DateTimePicker";
import {
import ScannerHeader from "../Scanner/ScannerHeader";
import ColumnScanHeaderAction from "../Scanner/ColumnScanHeaderAction";
import { scannerController } from "../Scanner/ScannerController";
import TableCell from "../common/TableCell";
import { parseDate } from "../../utils/dateUtils";
import tripService from "../../services/tripService";
function CompletePickupDialog( // [logic hidden]
```

### 📄 src/components/trips/CreateTripFromRouteDialog.jsx
```javascript
import { useEffect, useMemo, useState, useCallback } from "react";
import {
import { Autocomplete } from "@mui/material";
import ExpandMoreIcon from "@mui/icons-material/ExpandMore";
import AddIcon from "@mui/icons-material/Add";
import CloseIcon from "@mui/icons-material/Close";
import SearchIcon from "@mui/icons-material/Search";
import { DatePicker } from "@mui/x-date-pickers/DatePicker";
import { tripService } from "../../services/tripService";
import { userService } from "../../services/userService";
import { inventoryService } from "../../services/inventoryService";
import { useDcid } from "../../context/DcidContext";
import Tabs from "@mui/material/Tabs";
import Tab from "@mui/material/Tab";
export default function CreateTripFromRouteDialog( // [logic hidden]
```

### 📄 src/components/trips/TripDetailsSidebar.jsx
```javascript
import React, { useState, useEffect } from "react";
import {
import ExpandMoreIcon from "@mui/icons-material/ExpandMore";
import DriveEtaIcon from "@mui/icons-material/DriveEta";
import tripService from "../../services/tripService";
import { customerService } from "../../services/customerService";
import { laundryVendorService } from "../../services/laundryVendorService";
import { orderService } from "../../services/orderService";
import { debounce } from "lodash";
import {
import { DatePicker } from "@mui/x-date-pickers/DatePicker";
import { productService } from "../../services/productService";
import VisitImagesDialog from "./VisitImagesDialog";
import { userService } from "../../services/userService";
import { DATE_TIME, formatCustomDate } from "../../utils/dateUtils";
import CustomSnackbar from "../layout/CustomSnackbar";
import { GlobalStyles } from "@mui/material";
import TableCell from "../common/TableCell";
import CustomDrawer from "../common/CustomDrawer";
import CompleteDeliveryDialog from "./CompleteDeliveryDialog";
import CompletePickupDialog from "./CompletePickupDialog";
import VisitLevelChallanDialog from "./VisitLevelChallanDialog";
function TripDetailsSidebar( // [logic hidden]
              disabled // Automatically determined by trip type
              Parties are restricted by trip type ({tripDetails?.tripType === "WASH_TRIP" ? "Laundry Trip" : "Customer Trip"})
```

### 📄 src/components/trips/TripManagement.jsx
```javascript
import React, { useState, useEffect, useCallback, useMemo } from "react";
import {
import {
import { DatePicker } from "@mui/x-date-pickers/DatePicker";
import { subDays } from "date-fns";
import { tripService } from "../../services/tripService";
import { routeService } from "../../services/routeService";
import { inventoryService } from "../../services/inventoryService";
import { customerService } from "../../services/customerService";
import { laundryVendorService } from "../../services/laundryVendorService";
import { useDcid } from "../../context/DcidContext";
import TripDetailsSidebar from "./TripDetailsSidebar";
import CreateTripFromRouteDialog from "./CreateTripFromRouteDialog";
import CustomSnackbar from "../layout/CustomSnackbar";
import TripTimelineDialog from "./TripTimelineDialog";
import { formatCustomDate } from "../../utils/dateUtils";
function TripManagement() // [logic hidden]
              Restricted by trip type ({selectedTrip?.tripType === "WASH_TRIP" ? "Laundry Trip" : "Customer Trip"})
```

### 📄 src/components/trips/TripTimelineDialog.jsx
```javascript
import React, { useMemo } from "react";
import {
import { format, differenceInMinutes } from "date-fns";
import { parseDate } from "../../utils/dateUtils";
export default function TripTimelineDialog({ open, onClose, title, visits = [] }) // [logic hidden]
```

### 📄 src/components/trips/VisitImagesDialog.jsx
```javascript
import React, { useMemo, useState } from 'react';
import {
import { PermMedia } from '@mui/icons-material';
import CloseIcon from '@mui/icons-material/Close';
import Lightbox from 'yet-another-react-lightbox';
import Zoom from 'yet-another-react-lightbox/plugins/zoom';
import 'yet-another-react-lightbox/styles.css';
export default function VisitImagesDialog({ imageUrls = [], title = 'Visit Images' }) // [logic hidden]
```

### 📄 src/components/trips/VisitLevelChallanDialog.jsx
```javascript
import React, { useEffect, useRef, useState } from "react";
import {
import ImageUploadPanel from "../common/ImageUploadPanel";
export default function VisitLevelChallanDialog( // [logic hidden]
```

### 📄 src/components/users/AddUserDialog.jsx
```javascript
import React, { useState } from 'react';
import {
import {
function AddUserDialog({ open, onClose, onSave }) // [logic hidden]
```

### 📄 src/components/users/UserDetails.jsx
```javascript
import React from 'react';
import {
import {
import { format } from 'date-fns';
function UserDetails({ user, onClose }) // [logic hidden]
```

### 📄 src/components/users/UserDialog.jsx
```javascript
import React, { useState, useEffect } from 'react';
import {
function UserDialog({ open, onClose, onSave, user }) // [logic hidden]
```

### 📄 src/components/users/UserFormDialog.jsx
```javascript
import React, { useState, useEffect } from 'react';
import {
import { laundryUserService } from '../../services/laundryUserService';
function UserFormDialog({ open, onClose, onSave, user, errorMessage }) // [logic hidden]
```

### 📄 src/components/users/UserList.jsx
```javascript
import React, { useState, useEffect } from 'react';
import {
import {
import UserDetails from './UserDetails';
import UserFormDialog from './UserFormDialog';
import { laundryUserService } from '../../services/laundryUserService';
import CustomSnackbar from '../layout/CustomSnackbar';
function UserList() // [logic hidden]
```

### 📄 src/components/vehicles/VehicleManagement.jsx
```javascript
import React, { useState, useEffect } from 'react';
import {
import { Add as AddIcon, Delete as DeleteIcon } from '@mui/icons-material';
import { Search as SearchIcon } from '@mui/icons-material';
import { tripService } from '../../services/tripService';
function VehicleManagement() // [logic hidden]
    type: '',
            onChange={(e) => setVehicleData({ ...vehicleData, type: e.target.value })}
```

### 📄 src/components/vendors/ConfirmDeleteDialog.jsx
```javascript
import React from 'react';
import {
function ConfirmDeleteDialog({ open, onClose, onConfirm, vendorName }) // [logic hidden]
```

### 📄 src/components/vendors/CreateLaundryVendorDialog.jsx
```javascript
import React, { useState, useEffect } from 'react';
import {
function CreateLaundryVendorDialog({ open, onClose, onSave, vendor }) // [logic hidden]
```

### 📄 src/components/vendors/LaundryVendorDetails.jsx
```javascript
import React, { useEffect, useMemo, useState } from "react";
import {
import {
import { laundryVendorService } from "../../services/laundryVendorService";
import { routeService } from "../../services/routeService";
function AssignCustomersDialog({ open, onClose, vendor, onAssigned }) // [logic hidden]
function LaundryVendorDetails({ vendor, onClose }) // [logic hidden]
```

### 📄 src/components/vendors/LaundryVendorsPage.jsx
```javascript
import React, { useState, useEffect } from 'react';
import {
import {
import CreateLaundryVendorDialog from './CreateLaundryVendorDialog';
import CreateInwardRequestDialog from '../stocks/CreateInwardRequestDialog';
import LaundryVendorDetails from './LaundryVendorDetails';
import { laundryVendorService } from '../../services/laundryVendorService';
import { createInwardRequest } from '../../services/inwardRequestService';
import CustomSnackbar from '../layout/CustomSnackbar';
function LaundryVendorsPage() // [logic hidden]
```

### 📄 src/components/vendors/VendorDetails.jsx
```javascript
import React from 'react';
import {
import {
function VendorDetails({ vendor, onClose }) // [logic hidden]
```

### 📄 src/components/vendors/VendorDialog.jsx
```javascript
import React, { useState, useEffect } from 'react';
import {
function VendorDialog({ open, onClose, onSave, vendor }) // [logic hidden]
```

### 📄 src/components/vendors/VendorList.jsx
```javascript
import React, { useState, useEffect } from 'react';
import {
import {
import VendorDialog from './VendorDialog';
import VendorDetails from './VendorDetails';
import ConfirmDeleteDialog from './ConfirmDeleteDialog';
import { vendorService } from '../../services/vendorService';
import CustomSnackbar from '../layout/CustomSnackbar';
function VendorList() // [logic hidden]
```

### 📄 src/components/visits/AddVisitDialog.jsx
```javascript
import React from "react";
import {
import AddIcon from "@mui/icons-material/Add";
import DeleteIcon from "@mui/icons-material/Delete";
```

### 📄 src/components/washRequests/PendingItemsDashboard.jsx
```javascript
import React, { useEffect, useMemo, useState } from "react";
import {
import { BarChart, PieChart } from "@mui/x-charts";
import BarChartIcon from "@mui/icons-material/BarChart";
import ErrorIcon from "@mui/icons-material/Error";
import { washRequestService } from "../../services/washRequestService";
import { buildPendingItemsIndex } from "../../utils/pendingItemsTransformer";
import { getProductColor } from "../../utils/productColorRegistry";
export default function PendingItemsDashboard() // [logic hidden]
    async function load() // [logic hidden]
```

### 📄 src/components/washRequests/WashRequestsPage.jsx
```javascript
import React, { useState, useEffect, useCallback, useMemo } from "react";
import {
import CustomSnackbar from "../layout/CustomSnackbar";
import WashRequestTabs from "./filters/WashRequestTabs";
import WashFulfillmentTabs from "./filters/WashFulfillmentTabs";
import WashRequestTable from "./tables/WashRequestTable";
import WashFulfillmentTable from "./tables/WashFulfillmentTable";
import WashRequestDrawer from "./drawers/WashRequestDrawer";
import WashFulfillmentDrawer from "./drawers/WashFulfillmentDrawer";
import FulfillmentDialog from "./dialogs/FulfillmentDialog";
import WRUnifiedDialog from "./dialogs/WRUnifiedDialog";
import { washRequestService } from "../../services/washRequestService";
import { washFulfillmentService } from "../../services/washFulfillmentService";
import { subDays } from "date-fns";
import PendingItemsDashboard from "./PendingItemsDashboard";
function WashRequestsPage() // [logic hidden]
```

### 📄 src/components/washRequests/dialogs/FulfillmentDialog.jsx
```javascript
import React, { useCallback, useEffect, useMemo, useRef, useState } from "react";
import {
import { washFulfillmentService } from "../../../services/washFulfillmentService";
import { laundryVendorService } from "../../../services/laundryVendorService";
import { inventoryService } from "../../../services/inventoryService";
import { useDcid } from "../../../context/DcidContext";
import ColumnScanHeaderAction from "../../Scanner/ColumnScanHeaderAction";
import ScannerHeader from "../../Scanner/ScannerHeader";
import QuantityScanInput from "../../Scanner/QuantityScanInput";
import { scannerController } from "../../Scanner/ScannerController";
import KnockOffResultDialog from "./KnockOffResultDialog";
function FulfillmentDialog({ open, onClose, onSuccess }) // [logic hidden]
```

### 📄 src/components/washRequests/dialogs/KnockOffResultDialog.jsx
```javascript
import React, { useMemo } from "react";
import {
export default function KnockOffResultDialog( // [logic hidden]
```

### 📄 src/components/washRequests/dialogs/PopulateQuantityDialog.jsx
```javascript
import React, { useState } from 'react';
import {
function PopulateQuantityDialog({ open, onClose, onSubmit }) // [logic hidden]
```

### 📄 src/components/washRequests/dialogs/WRUnifiedDialog.jsx
```javascript
import React, {
import {
import WifiIcon from "@mui/icons-material/Wifi";
import WifiOffIcon from "@mui/icons-material/WifiOff";
import { laundryVendorService } from "../../../services/laundryVendorService";
import { inventoryService } from "../../../services/inventoryService";
import { soiledService } from "../../../services/soiledService";
import { washRequestService } from "../../../services/washRequestService";
import { productService } from "../../../services/productService";
import { useDcid } from "../../../context/DcidContext";
import ScannerHeader from "../../Scanner/ScannerHeader";
import QuantityScanInput from "../../Scanner/QuantityScanInput";
import ColumnScanHeaderAction from "../../Scanner/ColumnScanHeaderAction";
import { scannerController } from "../../Scanner/ScannerController";
export default function WRUnifiedDialog( // [logic hidden]
```

### 📄 src/components/washRequests/drawers/EditProductQuantitiesDialog.jsx
```javascript
import React from "react";
import {
function EditProductQuantitiesDialog( // [logic hidden]
```

### 📄 src/components/washRequests/drawers/LaundryProgress.jsx
```javascript
import React from "react";
import { Box, Typography, Divider } from "@mui/material";
function ProgressDonut({ washEfficiencyPercentage = 0, size = 160, accent = "#2f80ed", track = "#e9eef6", centerBg = "#ffffff" }) // [logic hidden]
function SummaryStats({ stats = [] }) // [logic hidden]
function MiniDonut({ washEfficiencyPercentage = 0, size = 64, accent = "#7bd389", track = "#f0f4fb", centerBg = "#ffffff" }) // [logic hidden]
function AttemptCircle({ washEfficiencyPercentage, label }) // [logic hidden]
export default function LaundryProgress({ washRequestData }) // [logic hidden]
```

### 📄 src/components/washRequests/drawers/WashFulfillmentDetails.jsx
```javascript
import React from "react";
import {
import { format, parseISO } from "date-fns";
import CloseIcon from "@mui/icons-material/Close";
import { soiledService } from "../../../services/soiledService";
import EditProductQuantitiesDialog from "./EditProductQuantitiesDialog";
import { DATE_TIME, formatCustomDate } from "../../../utils/dateUtils";
import TableCell from "../../common/TableCell";
function WashFulfillmentDetails({ fulfillment, onClose }) // [logic hidden]
```

### 📄 src/components/washRequests/drawers/WashFulfillmentDrawer.jsx
```javascript
import React from "react";
import CustomDrawer from "../../common/CustomDrawer";
import WashFulfillmentDetails from "./WashFulfillmentDetails";
function WashFulfillmentDrawer({ fulfillment, onClose }) // [logic hidden]
```

### 📄 src/components/washRequests/drawers/WashRequestDetails.jsx
```javascript
import { useState, useEffect, useMemo } from "react";
import {
import CloseIcon from "@mui/icons-material/Close";
import LaundryProgress from "./LaundryProgress.jsx";
import { washRequestService } from "../../../services/washRequestService.jsx";
import { washFulfillmentService } from "../../../services/washFulfillmentService.jsx";
import { DATE_TIME, formatCustomDate } from "../../../utils/dateUtils.js";
import { getStatusChipColor } from "../../../utils/statusUtils.js";
import { transformWashRequestAndFulfillment } from "../../../utils/washRequestTransformers.js";
import TableCell from "../../common/TableCell";
function WashRequestDetails({ request, onClose }) // [logic hidden]
```

### 📄 src/components/washRequests/drawers/WashRequestDrawer.jsx
```javascript
import React from "react";
import CustomDrawer from "../../common/CustomDrawer";
import WashRequestDetails from "./WashRequestDetails";
function WashRequestDrawer({ request, onClose }) // [logic hidden]
```

### 📄 src/components/washRequests/filters/WashFulfillmentTabs.jsx
```javascript
import React, { useEffect, useRef } from "react";
import {
import { DatePicker } from "@mui/x-date-pickers/DatePicker";
import { endOfDay, startOfDay } from "date-fns";
function WashFulfillmentTabs( // [logic hidden]
```

### 📄 src/components/washRequests/filters/WashRequestTabs.jsx
```javascript
import React, { useEffect, useRef } from "react";
import { Box, Button, MenuItem, Paper, TextField, Autocomplete } from "@mui/material";
import { DatePicker } from "@mui/x-date-pickers/DatePicker";
import { endOfDay, startOfDay } from "date-fns";
function WashRequestTabs( // [logic hidden]
```

### 📄 src/components/washRequests/tables/WashFulfillmentTable.jsx
```javascript
import React from "react";
import {
import { formatCustomDate } from "../../../utils/dateUtils";
function WashFulfillmentTable({ data = [], onSelect }) // [logic hidden]
```

### 📄 src/components/washRequests/tables/WashRequestTable.jsx
```javascript
import React from "react";
import {
import DeleteOutlineIcon from "@mui/icons-material/DeleteOutline";
import { washRequestService } from "../../../services/washRequestService";
import { formatCustomDate } from "../../../utils/dateUtils";
function WashRequestTable({ data, onSelect, onDeleted }) // [logic hidden]
```
